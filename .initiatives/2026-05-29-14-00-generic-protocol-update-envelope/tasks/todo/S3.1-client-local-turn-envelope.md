# S3.1: Add Generic Turn Envelopes to Client and Local Server

**Status**: TODO
**Wave**: W3
**Track**: Client/local transport
**Supports**: D4
**Effort**: M
**Dependencies**: S1.2

## Purpose

Enable the local single-player path to queue and send generic turn envelopes while preserving OpenFront's legacy turn handling through adapters.

## Scope
- In:
  - Adapt `LocalServer` intent stamping to generic stamped intent envelopes.
  - Bridge OpenFront UI intent events into generic envelopes.
  - Keep replay pause filtering and hash handling behavior intact.
- Out:
  - No remote server schema migration.
  - No new Foundation UI events.

## Touchpoints
- Modify:
  - `src/client/LocalServer.ts`
  - `src/client/Transport.ts`
  - Possibly `src/client/ClientGameRunner.ts`

## Acceptance Criteria
- [ ] Local OpenFront single-player still sends turns that reach OpenFront worker execution unchanged after bridge conversion.
- [ ] The local path can represent a non-OpenFront generic intent envelope in tests.

## Test Plan
- `local_server_stamps_generic_intent_envelope` - checks client ID stamping and turn order.
- `openfront_local_turn_bridge_preserves_legacy_intent` - checks OpenFront command intent payload survives.

## Working Notes
- Planning baseline created.
