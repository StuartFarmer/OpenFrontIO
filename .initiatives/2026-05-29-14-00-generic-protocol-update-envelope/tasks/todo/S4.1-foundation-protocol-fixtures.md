# S4.1: Add Foundation Protocol Fixtures

**Status**: TODO
**Wave**: W4
**Track**: Foundation readiness
**Supports**: D5
**Effort**: S
**Dependencies**: S2.2, S3.3

## Purpose

Prove a non-OpenFront game module can use generic protocol envelopes without importing OpenFront schemas or updates.

## Scope
- In:
  - Add compile-time or unit-test fixtures for `foundation` start config, click-to-place intent, and `foundation/player_placed` update event.
  - Use generic envelopes directly.
- Out:
  - No Foundation runtime, map, renderer, or gameplay implementation.

## Touchpoints
- Create:
  - `tests/core/protocol/foundation/*` or similar fixtures.
- Modify:
  - None outside tests unless protocol gaps are found.

## Acceptance Criteria
- [ ] Foundation fixtures compile with `moduleID: "foundation"`.
- [ ] Foundation fixtures do not import `src/core/game/GameUpdates.ts` or `src/core/Schemas.ts` OpenFront intent types.

## Test Plan
- `foundation_click_to_place_intent_uses_generic_envelope` - validates direct generic intent fixture.
- `foundation_player_placed_update_uses_module_event` - validates direct generic update fixture.

## Working Notes
- Planning baseline created.
