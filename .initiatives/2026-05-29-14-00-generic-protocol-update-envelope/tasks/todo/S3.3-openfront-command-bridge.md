# S3.3: Bridge Generic Intents into OpenFront Command Execution

**Status**: TODO
**Wave**: W3
**Track**: Command bridge
**Supports**: D4
**Effort**: M
**Dependencies**: S3.1, S3.2

## Purpose

Keep `IntentCommandSurface` and `Executor` working on OpenFront legacy intents while the transport can carry generic intent envelopes.

## Scope
- In:
  - Convert OpenFront generic stamped intent envelopes into legacy `StampedIntent` before `IntentCommandSurface`.
  - Add explicit errors for wrong module IDs or invalid OpenFront payloads.
  - Keep `Executor.createExecs` behavior equivalent for OpenFront turns.
- Out:
  - No Foundation command router.
  - No refactor of OpenFront execution classes.

## Touchpoints
- Modify:
  - `src/core/GameRunner.ts`
  - `src/core/execution/ExecutionManager.ts`
  - `src/core/systems/commands/IntentCommandSurface.ts` only if necessary.

## Acceptance Criteria
- [ ] Existing OpenFront command tests continue to pass.
- [ ] Wrong-module intents do not reach OpenFront execution.
- [ ] Generic turn hash field remains available for desync/replay logic.

## Test Plan
- `openfront_command_bridge_maps_generic_attack_intent` - checks execution creation parity.
- `openfront_command_bridge_rejects_wrong_module` - checks module isolation.

## Working Notes
- Planning baseline created.
