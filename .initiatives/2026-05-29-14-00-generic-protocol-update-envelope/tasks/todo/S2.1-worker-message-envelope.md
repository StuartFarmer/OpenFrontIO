# S2.1: Migrate Worker Messages to Generic Update Envelopes

**Status**: TODO
**Wave**: W2
**Track**: Worker updates
**Supports**: D3
**Effort**: M
**Dependencies**: S1.1, S1.3

## Purpose

Change worker update message payloads from `GameUpdateViewData` to `EngineUpdateEnvelope` while preserving batching and buffer transfers.

## Scope
- In:
  - Update `WorkerMessages.ts` update message types.
  - Update `Worker.worker.ts` batching to collect generic envelopes.
  - Use a protocol helper to collect transferable buffers.
  - Wrap OpenFront `GameRunner` callback output through the OpenFront update bridge.
- Out:
  - No intent/turn worker message changes beyond what compile requires.

## Touchpoints
- Create:
  - Transferable helper tests if not already created.
- Modify:
  - `src/core/worker/WorkerMessages.ts`
  - `src/core/worker/Worker.worker.ts`
  - Possibly `src/core/GameRunner.ts` callback type only if bridge placement requires it.

## Acceptance Criteria
- [ ] Worker posts `game_update_batch` with generic envelopes.
- [ ] Tile and motion typed arrays are transferred.
- [ ] Error updates continue to use the existing error path or a generic error envelope consistently.

## Test Plan
- `worker_update_batch_collects_engine_transferables` - checks transfer list content.
- `worker_update_batch_keeps_error_updates_out_of_success_batch` - checks current error behavior is preserved.

## Working Notes
- Planning baseline created.
