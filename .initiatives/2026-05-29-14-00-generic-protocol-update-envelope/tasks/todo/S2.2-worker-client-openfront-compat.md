# S2.2: Preserve OpenFront WorkerClient Compatibility

**Status**: TODO
**Wave**: W2
**Track**: Worker updates
**Supports**: D3
**Effort**: M
**Dependencies**: S2.1

## Purpose

Let existing OpenFront client code keep receiving `GameUpdateViewData` while the worker transport carries generic envelopes.

## Scope
- In:
  - Update `WorkerClient` to receive generic envelope messages.
  - Add an OpenFront-compatible callback path that unwraps generic envelopes through the bridge.
  - Consider a future generic callback API but keep existing OpenFront API stable for this wave.
- Out:
  - No Foundation worker client implementation.
  - No HUD migration.

## Touchpoints
- Modify:
  - `src/core/worker/WorkerClient.ts`
  - `src/client/ClientGameRunner.ts` only if callback typing requires a small adaptation.

## Acceptance Criteria
- [ ] `ClientGameRunner` can still call `worker.start((gu: GameUpdateViewData | ErrorUpdate) => ...)`.
- [ ] OpenFront bridge failures surface as errors rather than silent malformed updates.

## Test Plan
- `worker_client_unwraps_openfront_engine_update` - checks callback receives legacy shape.
- `worker_client_rejects_wrong_module_for_openfront_callback` - checks mismatched module envelope is handled explicitly.

## Working Notes
- Planning baseline created.
