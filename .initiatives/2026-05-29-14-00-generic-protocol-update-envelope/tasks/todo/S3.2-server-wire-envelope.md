# S3.2: Add Generic Envelopes to Server Wire Schemas

**Status**: TODO
**Wave**: W3
**Track**: Server transport
**Supports**: D4
**Effort**: L
**Dependencies**: S1.2

## Purpose

Prepare multiplayer-compatible server/client messages for generic starts, turns, and intents without weakening payload validation.

## Scope
- In:
  - Add generic server/client message schemas or versioned alternatives.
  - Preserve old OpenFront messages through bridge/default behavior.
  - Make module payload validation explicit at the module boundary.
  - Keep join/rejoin authentication and server-assigned client IDs unchanged.
- Out:
  - No multiplayer Foundation launch.
  - No lobby UI redesign.

## Touchpoints
- Modify:
  - `src/core/Schemas.ts` or new protocol schema files
  - `src/server/Worker.ts`
  - `src/server/GameManager.ts`
  - `src/server/GameServer.ts` if start/turn message creation requires adaptation

## Acceptance Criteria
- [ ] WebSocket parsing validates generic envelope shape.
- [ ] OpenFront create-game input remains accepted.
- [ ] Unknown module payloads are not accepted without a module validator.

## Test Plan
- `server_accepts_openfront_legacy_start_through_bridge` - preserves current behavior.
- `server_rejects_unvalidated_module_payload` - validates security boundary.
- `server_accepts_generic_turn_envelope_shape` - proves multiplayer-compatible format.

## Working Notes
- Planning baseline created.
