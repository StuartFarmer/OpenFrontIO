# S2.3: Re-run and Extend Worker Update Parity

**Status**: TODO
**Wave**: W2
**Track**: Worker updates
**Supports**: D3
**Effort**: S
**Dependencies**: S2.2

## Purpose

Verify the generic worker update transport did not change OpenFront-observable update behavior.

## Scope
- In:
  - Update `WorkerClientUpdateParity.test.ts` only as needed to account for the bridge.
  - Add assertions for generic envelope internals where useful.
- Out:
  - No new gameplay behavior.

## Touchpoints
- Modify:
  - `tests/core/systems/WorkerClientUpdateParity.test.ts`
  - `tests/util/parity/*` only if necessary.

## Acceptance Criteria
- [ ] Existing parity assertions for updates, packed tile updates, packed motion plans, pending turns, tick, and player name data remain meaningful.
- [ ] Focused parity test passes.

## Test Plan
- `worker_update_parity_scripted_game` - preserve current OpenFront parity.
- `worker_update_parity_engine_envelope_bridge` - optional new assertion around bridge internals.

## Working Notes
- Planning baseline created.
