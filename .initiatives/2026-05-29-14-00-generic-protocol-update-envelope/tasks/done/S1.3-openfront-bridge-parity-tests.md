# S1.3: Add OpenFront Bridge Parity Tests

**Status**: DONE
**Wave**: W1
**Track**: Join
**Supports**: D2
**Effort**: M
**Dependencies**: S1.1, S1.2

## Purpose

Prove the bridge preserves the behavior existing tests care about before runtime code starts sending generic envelopes.

## Scope
- In:
  - Extend or add parity tests around update snapshots.
  - Reuse `tests/util/parity/ParitySnapshot.ts` where practical.
  - Include player diff/merge fixtures with bridged update payloads.
- Out:
  - No worker message migration.

## Touchpoints
- Create:
  - `tests/core/protocol/openfront/*`
- Modify:
  - Existing parity helpers only if necessary and backward compatible.

## Acceptance Criteria
- [x] Bridge parity tests compare normalized legacy payloads before and after generic wrapping.
- [x] Existing `tests/GameUpdateUtils.test.ts` remains unchanged unless helper imports move.

## Test Plan
- `openfront_bridge_preserves_parity_snapshot` - compares normalized update snapshots.
- `openfront_bridge_preserves_partial_player_updates` - checks partial player update payloads survive wrapping.

## Working Notes
- Planning baseline created.
- 2026-05-29: Scope confirmed for parity-only coverage under `tests/core/protocol/openfront`; existing `tests/GameUpdateUtils.test.ts` remains unchanged.
- 2026-05-29: Added normalized update parity coverage and partial player diff/merge bridge coverage.
- 2026-05-29: Verified with `npx vitest run tests/core/protocol` and `npx tsc --noEmit`.
