# S1.2: Build OpenFront Start, Turn, and Intent Bridge

**Status**: DONE
**Wave**: W1
**Track**: Start/turn/intent bridge
**Supports**: D2, D4
**Effort**: M
**Dependencies**: S0.1

## Purpose

Adapt legacy OpenFront start and turn schemas to generic envelopes while keeping OpenFront command code unchanged.

## Scope
- In:
  - Convert `GameStartInfo` to/from `GameStartEnvelope`.
  - Convert `Turn` to/from `TurnEnvelope`.
  - Convert `StampedIntent` to/from `StampedIntentEnvelope`.
  - Default missing `moduleID` to `openfront` for legacy starts and records.
- Out:
  - No server WebSocket schema migration yet.
  - No changes to `IntentCommandSurface`.

## Touchpoints
- Create:
  - OpenFront protocol bridge files
- Modify:
  - Protocol exports as needed.

## Acceptance Criteria
- [x] Existing OpenFront intent objects round-trip with no payload changes.
- [x] Generic envelopes do not require OpenFront-specific fields outside payloads.

## Test Plan
- `openfront_turn_bridge_round_trips_stamped_intents` - checks client ID, turn number, hash, and intent fields.
- `legacy_game_start_defaults_module_id_to_openfront` - checks old starts have a stable module identity.

## Working Notes
- Planning baseline created.
- 2026-05-29: Scope confirmed for isolated OpenFront start/turn/intent bridge under `src/core/protocol/openfront`; existing wire schemas and command surfaces remain unchanged.
- 2026-05-29: Implemented GameStartInfo, Turn, and StampedIntent adapters. Legacy records default missing module identity to `openfront` and preserve intent objects as module-owned payloads.
- 2026-05-29: Verified with `npx vitest run tests/core/protocol/openfront/OpenFrontStartTurnIntentBridge.test.ts`.
