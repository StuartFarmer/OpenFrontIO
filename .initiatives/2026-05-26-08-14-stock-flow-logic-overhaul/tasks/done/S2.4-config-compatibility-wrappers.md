# S2.4: Config Compatibility Wrappers

**Status**: DONE
**Wave**: W2
**Track**: Join
**Supports**: D2, D3
**Effort**: L
**Dependencies**: S2.2, S2.3

## Purpose

Keep existing `Config` economy APIs working while delegating calculation to
stock-flow systems where practical.

## Scope

- In: Compatibility path for `maxTroops`, `effectiveTroopCapacity`,
  `troopIncreaseRate`, `maxResources`, `resourceIncreaseRate`, and biomass
  diagnostic compatibility.
- Out: Renaming public APIs and changing player update payloads.

## Touchpoints

- Modify: `src/core/configuration/Config.ts`
- Modify: `tests/core/configuration/ResourceCapacity.test.ts`
- Modify: `tests/core/executions/PlayerExecution.test.ts`

## Acceptance Criteria

- [x] Existing tests for population/resource formulas pass.
- [x] Existing call sites compile without API changes.
- [x] Compatibility wrappers are documented in code comments only where needed.

## Test Plan

- `ResourceCapacity.test.ts` - existing config parity.
- `PlayerExecution.test.ts` - existing passive income and growth behavior.
- `npx tsc --noEmit` - broad call-site compatibility.

## Working Notes

- Planning baseline created.
- Updated `Config.maxTroops`, `Config.troopIncreaseRate`,
  `Config.maxResources`, and `Config.resourceIncreaseRate` to delegate through
  stock-flow-backed adapter helpers while preserving public API shape.
- Updated stale execution test language from biomass-constrained growth to max
  population from owned tiles.
- Verified with targeted systems/config/execution tests and `npx tsc --noEmit`.
