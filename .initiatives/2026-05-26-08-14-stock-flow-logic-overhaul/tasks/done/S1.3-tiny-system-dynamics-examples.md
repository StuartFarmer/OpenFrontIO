# S1.3: Tiny System Dynamics Examples

**Status**: DONE
**Wave**: W1
**Track**: Foundation
**Supports**: D1
**Effort**: S
**Dependencies**: S1.2

## Purpose

Prove the runtime with small Vensim-style examples before touching gameplay.

## Scope

- In: Test-only models for `Money = INTEG(interest, 100)` and
  `Workers = INTEG(-attrition, 100)`.
- Out: Vensim parser, external model import, solver options.

## Touchpoints

- Create: `tests/core/systems/StockFlowExamples.test.ts`
- Modify: None expected.

## Acceptance Criteria

- [x] Money model increases by `money * interestRate` per tick.
- [x] Workers model decreases by `workers / averageTenancy` per tick.
- [x] Tests verify one tick equals one model step.

## Test Plan

- `StockFlowExamples.test.ts` - tiny stock-flow example behavior.

## Working Notes

- Planning baseline created.
- Added `StockFlowExamples.test.ts` with Money and Workers models.
- Verified with `npx vitest run tests/core/systems`.
