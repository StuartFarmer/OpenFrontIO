# S2.3: Resource Production System Parity

**Status**: DONE
**Wave**: W2
**Track**: Resources
**Supports**: D2
**Effort**: L
**Dependencies**: S2.1

## Purpose

Represent current resource capacity, terrain-weighted passive production, and
capacity clamping through stock-flow outputs and flows.

## Scope

- In: `ResourceProductionSystem` for food/energy/materials regeneration,
  capacity, terrain split, and player multipliers.
- Out: Explicit food consumption and wartime demand.

## Touchpoints

- Create: `src/core/systems/models/ResourceProductionSystem.ts`
- Create: `tests/core/systems/ResourceProductionSystem.test.ts`
- Modify: `src/core/systems/models/PlayerEconomyModel.ts`

## Acceptance Criteria

- [x] `resource.capacity.*` matches `Config.maxResources(...)`.
- [x] Passive regen flow matches `Config.resourceIncreaseRate(...)`.
- [x] Terrain split matches current plains/highland/mountain weight behavior.
- [x] Capacity clamping matches `clampResourceDeltaToCapacity(...)`.

## Test Plan

- `ResourceProductionSystem.test.ts` - capacity, terrain, regen, and clamp
  parity.
- Existing `ResourceCapacity.test.ts` resource cases remain passing when
  wrappers are connected.

## Working Notes

- Planning baseline created.
- Added `ResourceProductionSystem` for current resource capacity and passive
  terrain-weighted regeneration parity.
- Preserved existing over-cap behavior by clamping only positive deltas rather
  than clamping existing stored resources down to capacity.
