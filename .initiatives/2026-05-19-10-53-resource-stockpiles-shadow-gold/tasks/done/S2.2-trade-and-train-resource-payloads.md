# S2.2: Trade And Train Resource Payloads

**Status**: DONE
**Wave**: W2
**Track**: Acquisition A
**Supports**: D2
**Effort**: Medium
**Dependencies**: S1.2, S2.1

## Purpose

Migrate trade ship and train station rewards to resource payloads while preserving current reward values and event/stat behavior.

This ticket changes player mutations only. Display messages, stats methods, and reward calculation names remain gold-shaped for compatibility.

## Scope
- In: Trade ship normal rewards, captured trade ship rewards, train station self/external rewards, and focused test expectations.
- Out: Trade market mechanics, resource-specific route values, UI popup redesign.

## Touchpoints
- Create: None expected.
- Modify: `src/core/execution/TradeShipExecution.ts`, `src/core/game/TrainStation.ts`.

## Acceptance Criteria
- [x] Trade ship rewards use `addResources(resourcesFromGoldAmount(gold), tile?)`.
- [x] Train station rewards use `addResources(resourcesFromGoldAmount(gold), tile?)`.
- [x] Existing display messages and stats remain gold-compatible.
- [x] Tests assert Food, Energy, and Materials receive the old reward amount.

## Test Plan
- `npx vitest run tests/core/executions/TradeShipExecution.test.ts tests/core/game/TrainStation.test.ts` - verifies trade/train reward compatibility.

## Working Notes
- Planning baseline created.
- Hardened ticket before implementation.
- Migrated `TradeShipExecution` rewards to `addResources(resourcesFromGoldAmount(gold), tile?)`.
- Migrated `TrainStation` trade rewards to `addResources(resourcesFromGoldAmount(gold), tile?)`.
- Updated trade and train tests to assert resource payloads while leaving gold-compatible stats/messages intact.
- Validation: `npx vitest run tests/core/executions/TradeShipExecution.test.ts tests/core/game/TrainStation.test.ts` passed.
