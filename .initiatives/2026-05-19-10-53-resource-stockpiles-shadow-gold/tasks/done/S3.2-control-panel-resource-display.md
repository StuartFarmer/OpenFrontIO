# S3.2: Control Panel Resource Display

**Status**: DONE
**Wave**: W3
**Track**: UI
**Supports**: D3, D5
**Effort**: Medium
**Dependencies**: S3.1

## Purpose

Make the resource stockpiles visible during play with a compact first-pass UI.

## Scope
- In: Add compact Food, Energy, and Materials values to the in-game control panel for desktop and mobile.
- Out: Full economy UI redesign, new icons/assets, localization overhaul, replacing all gold displays, affordability changes.

## Touchpoints
- Create: None expected.
- Modify: `src/client/hud/layers/ControlPanel.ts`.

## Acceptance Criteria
- [x] Control panel shows Food, Energy, and Materials values.
- [x] Display fits existing desktop/mobile control panel constraints.
- [x] Existing troop/gold display behavior is not broken.
- [x] Values update as player state changes.
- [x] Gold remains visible for compatibility during this phase.

## Test Plan
- `npx tsc --noEmit` - verifies the UI compiles against `PlayerView.resources()`.
- `npx vitest run tests/GameUpdateUtils.test.ts tests/client/view/PlayerView.test.ts` - keeps the resource state contract covered until a dedicated UI smoke test is added.
- Manual visual check at the W3 playtest stop.

## Working Notes
- Keep the first-pass UI text-only and compact so no new assets or localization work is required for this checkpoint.
- Added a compact desktop/mobile resource strip beside the existing gold and troop controls.
- `ControlPanel.tick()` now reads `player.resources()` each tick, so visible values follow player update state.
- Validation passed: `npx tsc --noEmit`.
- Validation passed: `npx vitest run tests/GameUpdateUtils.test.ts tests/client/view/PlayerView.test.ts`.
