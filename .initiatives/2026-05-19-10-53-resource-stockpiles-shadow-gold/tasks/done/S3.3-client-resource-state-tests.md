# S3.3: Client Resource State Tests

**Status**: DONE
**Wave**: W3
**Track**: Join
**Supports**: D3, D4
**Effort**: Small
**Dependencies**: S3.1, S3.2

## Purpose

Add focused tests for resource update propagation and minimal UI visibility.

## Scope
- In: Update utility tests, PlayerView tests, and lightweight control panel render tests if existing test infrastructure supports it.
- Out: Browser/e2e tests.

## Touchpoints
- Create: `tests/client/hud/ControlPanel.test.ts`.
- Modify: `tests/GameUpdateUtils.test.ts`, `tests/client/view/PlayerView.test.ts`.

## Acceptance Criteria
- [x] Resource diffs are emitted only when resource values change.
- [x] Applying partial player updates updates resource state.
- [x] `PlayerView.resources()` returns expected values.
- [x] UI smoke test covers the control panel display.

## Test Plan
- `npx vitest run tests/GameUpdateUtils.test.ts tests/client/view/PlayerView.test.ts tests/client/hud/ControlPanel.test.ts` - verifies client state propagation and resource UI visibility.
- `npx tsc --noEmit` - verifies test and UI type contracts.

## Working Notes
- State/view tests were added with S3.1; this ticket adds the dedicated control-panel smoke coverage.
- Added `tests/client/hud/ControlPanel.test.ts` to verify Food, Energy, and Materials render for the local player.
- Extended the view test stubs with resource defaults, localStorage support, and control-panel config methods.
- Validation passed: `npx vitest run tests/GameUpdateUtils.test.ts tests/client/view/PlayerView.test.ts tests/client/hud/ControlPanel.test.ts`.
- Validation passed: `npx tsc --noEmit`.
