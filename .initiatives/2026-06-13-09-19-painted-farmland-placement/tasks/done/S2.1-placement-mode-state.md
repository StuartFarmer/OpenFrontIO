# S2.1: Placement Mode State

**Status**: DOING
**Wave**: W2
**Track**: Client
**Supports**: D3
**Effort**: M
**Dependencies**: S1.1, S1.3

## Purpose

Introduce explicit state that distinguishes normal ghost placement from paint placement.

## Scope

- In: Placement metadata/helper, UI state additions, paintable type detection, selected-mode clearing rules.
- Out: Pointer event routing and UI rendering.

## Touchpoints

- Create: `src/client/controllers` helper or shared placement metadata module if appropriate.
- Modify: `src/client/UIState.ts`, `src/client/controllers/BuildPreviewController.ts`, `src/client/InputHandler.ts`.

## Acceptance Criteria

- [x] Farmland selection is identified as paint mode without changing normal build behavior.
- [x] Selecting a non-paint build item exits paint mode.
- [x] Escape clears both ghost and paint state.

## Test Plan

- `test_placement_mode_switching` - paint vs normal mode state transitions.
- `test_escape_clears_paint_state` - cancellation cleanup.

## Working Notes

- Planning baseline created.
- Added `src/client/controllers/PlacementMode.ts` with explicit `single` vs `paint` metadata.
- Farmland is the only paintable build unit; existing structures remain single-placement.
- Added `tests/client/controllers/PlacementMode.test.ts`.
- Verified with focused Vitest and `npx tsc --noEmit`.
