# S3.2: Regression Test Pass

**Status**: DOING
**Wave**: W3
**Track**: Verification
**Supports**: D5
**Effort**: M
**Dependencies**: S1.2, S2.3, S3.1

## Purpose

Run and stabilize the focused automated checks for core placement, client paint behavior, and build/typecheck coverage.

## Scope

- In: Relevant Vitest suites, build/typecheck, failing-test triage caused by the feature.
- Out: Broad unrelated refactors.

## Touchpoints

- Create: Additional focused regression tests only if gaps are found.
- Modify: Tests and implementation files needed to make feature-owned failures pass.

## Acceptance Criteria

- [x] Focused core Farmland tests pass.
- [x] Focused client paint-mode tests pass.
- [x] `npm run build-dev` passes.

## Test Plan

- `npm test -- <focused farmland/core tests>` - core coverage.
- `npm test -- <focused client/controller tests>` - paint UX coverage.
- `npm run build-dev` - type/build coverage.

## Working Notes

- Planning baseline created.
- Focused regression command passed: `npx vitest run tests/client/hud/BuildBar.test.ts tests/client/controllers/PlacementMode.test.ts tests/client/controllers/BuildPreviewController.test.ts tests/PlayerImpl.test.ts tests/core/configuration/ResourceCapacity.test.ts`.
- Full type/build command passed: `npm run build-dev`.
- Note: `npm test -- ...` runs the repository's full suite before appended paths and currently exposes unrelated dirty-worktree failures; focused `npx vitest run` was used for feature verification.
