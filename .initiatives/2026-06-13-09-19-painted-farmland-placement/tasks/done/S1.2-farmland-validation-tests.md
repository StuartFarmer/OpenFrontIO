# S1.2: Farmland Validation Tests

**Status**: DOING
**Wave**: W1
**Track**: Core
**Supports**: D1, D5
**Effort**: M
**Dependencies**: S1.1

## Purpose

Pin the authoritative placement rules for Farmland.

## Scope

- In: Owned-land validation, invalid terrain rejection, duplicate tile rejection, insufficient-resource handling, disabled-unit behavior.
- Out: Client paint controller tests.

## Touchpoints

- Create: New focused test file if existing tests do not fit.
- Modify: `tests/core/game/PlayerImpl.test.ts`, `tests/core/configuration/ResourceCapacity.test.ts`, or equivalent core test files.

## Acceptance Criteria

- [x] Tests fail before the Farmland placement rules are implemented and pass after.
- [x] Duplicate farmland placement on one tile is rejected.
- [x] Existing structure placement tests continue to pass.

## Test Plan

- `npm test -- tests/core/game/PlayerImpl.test.ts tests/core/configuration/ResourceCapacity.test.ts` - core placement/config regression.

## Working Notes

- Planning baseline created.
- Added coverage in `tests/PlayerImpl.test.ts` for one-tile owned-land placement, duplicate rejection, unowned tile rejection, and disabled-unit rejection.
- Added coverage in `tests/core/configuration/ResourceCapacity.test.ts` for Farmland resource cost split.
- Verified with `npx vitest run tests/PlayerImpl.test.ts tests/core/configuration/ResourceCapacity.test.ts`.
