# S1.3: Farmland Render Preview

**Status**: DOING
**Wave**: W1
**Track**: Render
**Supports**: D2
**Effort**: M
**Dependencies**: S1.1

## Purpose

Make Farmland visible as a built 1x1 tile and as a placement preview using existing tile hover affordances.

## Scope

- In: Renderer metadata, sprite/tile visual, ghost preview support, hover valid/invalid treatment.
- Out: Brush cursor and drag painting.

## Touchpoints

- Create: Minimal Farmland asset or renderer style if needed.
- Modify: `src/client/render/types`, `src/client/render/gl`, `src/client/hud/SpriteLoader.ts`, and `src/client/controllers/BuildPreviewController.ts` as needed.

## Acceptance Criteria

- [x] Built Farmland renders at tile scale.
- [x] Farmland ghost preview is tile-aligned and distinguishable from invalid hover state.
- [x] Existing structure ghost previews are unchanged.

## Test Plan

- `npm run build-dev` - compile and renderer integration check.
- `test_farmland_ghost_preview_data` - verifies preview metadata if controller helper tests exist or are added.

## Working Notes

- Planning baseline created.
- Added `UT_FARMLAND` to renderer structure type lists and canonical unit order.
- Added Farmland to `StructurePass`/`StructureLevelPass` structure order with a compact diamond/square marker and no atlas icon requirement.
- Added render settings for Farmland and a ghost-preservation regression assertion.
- Verified with `npx vitest run tests/client/controllers/BuildPreviewController.test.ts tests/PlayerImpl.test.ts tests/core/configuration/ResourceCapacity.test.ts`.
- Verified with `npm run build-dev`.
