# S3.1: Bottom Build UI Integration

**Status**: DOING
**Wave**: W3
**Track**: UI
**Supports**: D4
**Effort**: M
**Dependencies**: S2.3, S2.4

## Purpose

Expose Farmland in the bottom building UI as the primary entry point for paint mode.

## Scope

- In: Bottom build item metadata, icon/label/tooltip, selected/disabled/affordability states, paint-mode activation.
- Out: Replacing unrelated HUD controls.

## Touchpoints

- Create: Farmland icon/metadata if missing.
- Modify: `src/client/hud/layers/UnitDisplay.ts` and/or files introduced by `.initiatives/2026-06-13-09-14-rts-bottom-build-bar`.

## Acceptance Criteria

- [x] Farmland appears in the bottom build area after spawn for alive players.
- [x] Clicking Farmland enters paint mode and displays selected state.
- [x] Farmland is not added to the old right-click build menu as its primary workflow.

## Test Plan

- `test_farmland_bottom_button_enters_paint_mode` - UI/controller state integration.
- `npm run build-dev` - compile and Vite build check.

## Working Notes

- Planning baseline created.
- Added Farmland-specific metadata directly to `BuildBar`, not to `BuildMenu`.
- Added BuildBar tests for Farmland visibility, disabled filtering, and selection into `uiState.ghostStructure`.
- Added a shared Foundation building catalog so the Foundation page's separate `renderBuildBar()` also shows Farmland from a single game-level building list.
- Verified with `npx vitest run tests/client/hud/BuildBar.test.ts tests/client/controllers/PlacementMode.test.ts tests/client/controllers/BuildPreviewController.test.ts tests/PlayerImpl.test.ts tests/core/configuration/ResourceCapacity.test.ts`.
- Verified with `npm run build-dev`.
