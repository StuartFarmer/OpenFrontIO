# S3.3: Manual Smoke And Docs

**Status**: DOING
**Wave**: W3
**Track**: Verification
**Supports**: D5
**Effort**: S
**Dependencies**: S3.1, S3.2

## Purpose

Verify the feature in a running game and update any lightweight documentation or initiative notes needed for handoff.

## Scope

- In: Manual smoke checklist, screenshots or notes if useful, docs/help copy touched by Farmland or bottom UI.
- Out: Full product documentation rewrite.

## Touchpoints

- Create: Smoke notes in the initiative if useful.
- Modify: `docs/` or localization/help files only where user-facing text changes require it.

## Acceptance Criteria

- [ ] Manual smoke confirms paint start, repeat placement, release, right-click cancel, Escape cancel, and selected-state behavior.
- [ ] Manual smoke confirms camera drag, radial/context menu, and normal build placement still work.
- [ ] Any user-facing text added for Farmland is localized or queued according to repo convention.

## Test Plan

- `npm run dev` - manual gameplay smoke in browser.
- `npm test` - final broad regression if feature impact warrants it.

## Working Notes

- Planning baseline created.
- User reported the visible bottom bar showed Foundation items (`Grain Silo`, `Oil Tank`) but not Farmland. Root cause: Foundation has a separate `renderBuildBar()` path from OpenFront's `BuildBar` custom element.
- Added a shared Foundation building catalog and wired Foundation `renderBuildBar()` to it. The catalog marks Farmland as a 1x1 paint placement and storage buildings as 4x4 single placement.
- Added Foundation client coverage for Farmland appearing in the bottom bar and painting multiple 1x1 tiles while remaining selected.
- Verified with `npx vitest run tests/games/foundation/client/FoundationPage.test.ts tests/games/foundation/runtime.test.ts`.
- Verified with `npx tsc --noEmit`.
- Verified with `npm run build-dev`.
- Follow-up behavior: the first invalid tile reached during a Farmland paint stroke cancels paint mode and continues the same pointer gesture as map drag. Re-entering valid land does not resume painting; the player must select Farmland again. Covered in Foundation page tests and shared `InputHandler` tests.
