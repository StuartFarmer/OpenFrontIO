# S1.4: HUD Shell Placement

**Status**: DONE
**Wave**: W1
**Track**: Integration
**Supports**: D1
**Effort**: S
**Dependencies**: S1.2, S1.3

## Purpose

Mount the build bar into the active game HUD shell without colliding with existing bottom controls.

## Scope

- In: Live game shell placement, sandbox shell placement if applicable, layer/controller initialization.
- Out: Broad HUD redesign.

## Touchpoints

- Modify: `src/client/Main.ts`
- Modify: Any game bootstrap code that wires HUD controllers/layers if the component needs controller lifecycle hooks.

## Acceptance Criteria

- [x] Build bar appears in regular game and relevant sandbox shell paths.
- [x] Layout does not cover `control-panel`, `unit-display`, `attacks-display`, chat, or events in common viewport sizes.

## Test Plan

- `test_main_renders_build_bar_in_game_shell` - shell markup test if existing coverage supports it.
- Manual smoke: desktop 1440px and mobile-width viewport.

## Working Notes

- Replaced the live shell bottom-center `unit-display` with `build-bar` in `index.html` and the sandbox shell in `src/client/Main.ts`.
- Wired `build-bar` into `src/games/openfront/client/OpenFrontHud.ts` as a lifecycle-managed HUD layer.
- Verified shell integration with `npm run build-dev`.
