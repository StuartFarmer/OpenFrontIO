# S1.2: Persistent Build Bar UI

**Status**: DONE
**Wave**: W1
**Track**: UI
**Supports**: D1
**Effort**: M
**Dependencies**: S1.1

## Purpose

Create the Lit component that renders the RTS-style bottom build bar.

## Scope

- In: Component markup/styles, active state, disabled state, cost display, count/capacity hint if useful.
- Out: New core unit types or resource balancing.

## Touchpoints

- Create: `src/client/hud/layers/BuildBar.ts` or similarly named component.
- Modify: HUD imports/registration where live HUD layers are imported.

## Acceptance Criteria

- [x] Component renders a compact bottom command bar suitable for desktop and mobile.
- [x] Buttons do not leak click events to the map canvas.
- [x] Active selected building is visually distinct.

## Test Plan

- `test_build_bar_renders_requested_buttons` - component-level DOM test.
- `test_build_bar_stops_pointer_propagation` - component-level event test.

## Working Notes

- Added `src/client/hud/layers/BuildBar.ts` as a Lit HUD layer using existing HUD unit button primitives.
- Pointer/click/contextmenu propagation is stopped at the bar boundary and covered by `tests/client/hud/BuildBar.test.ts`.
- Active ghost selection is reflected through the existing `hud-unit-button` active state.
