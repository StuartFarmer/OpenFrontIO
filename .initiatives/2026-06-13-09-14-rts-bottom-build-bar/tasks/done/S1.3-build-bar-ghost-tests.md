# S1.3: Build Bar Ghost Tests

**Status**: DONE
**Wave**: W1
**Track**: Interaction contract
**Supports**: D2
**Effort**: M
**Dependencies**: S1.1

## Purpose

Cover the contract between bottom-bar selection and the existing ghost placement controller.

## Scope

- In: Tests that bar selection sets `uiState.ghostStructure`, toggles active selection, and clears selection consistently.
- Out: Full browser playtest automation.

## Touchpoints

- Create: `tests/client/hud/BuildBar.test.ts` or equivalent.
- Modify: Existing `tests/client/controllers/BuildPreviewController.test.ts` only if additional helper coverage is needed.

## Acceptance Criteria

- [x] Selecting an existing structure enters ghost mode.
- [x] Selecting an existing buildable attack enters ghost mode if attacks are included in the chosen bar scope.
- [x] Escape/right-click cancellation remains owned by existing input/preview flow.

## Test Plan

- `test_select_build_button_sets_ghost_structure` - validates UI state mutation.
- `test_reselect_active_build_button_clears_or_keeps_expected_state` - locks chosen toggle behavior.

## Working Notes

- Added `tests/client/hud/BuildBar.test.ts` for ghost selection and reselect-to-clear behavior.
- Build bar selection only mutates `uiState.ghostStructure`; confirmation and cancellation remain in the existing map input and preview flow.
