# S2.2: Drag Command Dispatch

**Status**: DONE
**Wave**: W2
**Track**: Client gesture
**Supports**: D3
**Effort**: M
**Dependencies**: S2.1, S2.3

## Purpose

Dispatch the correct Foundation grow command for plain click versus completed attack drag.

## Scope

- In: Plain click sends uniform mode; attack drag sends focused mode with release target and focus value.
- Out: Tuning text and final preview rendering.

## Touchpoints

- Create: None.
- Modify: `src/games/foundation/client/FoundationPage.ts`, `tests/games/foundation/client/FoundationPage.test.ts`.

## Acceptance Criteria

- [ ] Plain post-placement click dispatches `frontMode: "uniform"` and `frontFocus: 0`.
- [ ] Drag release dispatches `frontMode: "focused"` with normalized focus.
- [ ] Suppressed click handling does not double-dispatch after drag.

## Test Plan

- `FoundationPage click dispatch test` - verifies uniform command payload.
- `FoundationPage drag dispatch test` - verifies focused command payload and no duplicate click.

## Working Notes

- Planning baseline created.
- 2026-06-08: Plain post-placement click now dispatches `frontMode: "uniform"` with `frontFocus: 0`.
- 2026-06-08: Owned-territory drag release dispatches `frontMode: "focused"` with normalized focus and suppresses duplicate click dispatch.
- 2026-06-08: Verified by `npx vitest run tests/games/foundation/client/FoundationPage.test.ts`.
