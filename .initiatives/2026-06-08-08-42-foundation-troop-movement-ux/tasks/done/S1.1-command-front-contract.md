# S1.1: Command Front Contract

**Status**: DONE
**Wave**: W1
**Track**: Runtime contract
**Supports**: D1
**Effort**: M
**Dependencies**: None

## Purpose

Make front behavior explicit in Foundation grow commands so UI gestures can choose broad or focused expansion without relying on clicked-target distance.

## Scope

- In: Add serializable front-control fields to command types, command factory options, and command router forwarding.
- Out: Domain weighting implementation and client pointer gestures.

## Touchpoints

- Create: None.
- Modify: `src/games/foundation/runtime/FoundationProtocol.ts`, `src/games/foundation/runtime/FoundationCommands.ts`, `src/games/foundation/runtime/FoundationCommandRouter.ts`.

## Acceptance Criteria

- [ ] `FoundationGrowTerritoryCommand` can represent uniform and focused front requests.
- [ ] Missing fields default safely for existing callers.
- [ ] Invalid focus values are normalized or clamped before domain use.

## Test Plan

- `runtime command factory test` - command options serialize expected front fields.
- `router forwarding test` - router passes front options into `startWildernessExploration`.

## Working Notes

- Planning baseline created.
- 2026-06-08: Started execution as part of W1 runtime front contract.
- 2026-06-08: Added `frontMode` and `frontFocus` to grow command creation, protocol payloads, router forwarding, and persisted exploration intent.
- 2026-06-08: Verified through `npx vitest run tests/games/foundation/runtime.test.ts`.
