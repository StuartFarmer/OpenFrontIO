# S1.3: Runtime Behavior Tests

**Status**: DONE
**Wave**: W1
**Track**: Runtime contract
**Supports**: D1, D2
**Effort**: M
**Dependencies**: S1.2

## Purpose

Update and extend runtime tests so the intended UX semantics are enforced at simulation level.

## Scope

- In: Rewrite obsolete distance-click tests and add explicit uniform/focused assertions.
- Out: DOM pointer tests.

## Touchpoints

- Create: None.
- Modify: `tests/games/foundation/runtime.test.ts`, `tests/games/foundation/wilderness_elevation.test.ts` if distance helper tests change.

## Acceptance Criteria

- [ ] Existing “concentrates near close clicked targets” expectation is replaced with uniform click behavior.
- [ ] Focused mode remains directional and concentration-sensitive.
- [ ] Front capacity test still proves velocity saturation.

## Test Plan

- `npx vitest run tests/games/foundation/runtime.test.ts` - runtime behavior coverage.
- `npx vitest run tests/games/foundation/wilderness_elevation.test.ts` - helper and terrain regressions.

## Working Notes

- Planning baseline created.
- 2026-06-08: Replaced the stale distance-click concentration assertion with separate uniform default and explicit focused-mode coverage.
- 2026-06-08: `npx vitest run tests/games/foundation/runtime.test.ts` passes.
- 2026-06-08: `npx vitest run tests/games/foundation/wilderness_elevation.test.ts` passes.
