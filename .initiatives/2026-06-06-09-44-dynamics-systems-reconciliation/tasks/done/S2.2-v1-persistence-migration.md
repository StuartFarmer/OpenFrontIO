# S2.2: V1 Persistence Migration

**Status**: DONE
**Wave**: W2
**Track**: Persistence
**Supports**: D2
**Effort**: Medium
**Dependencies**: S2.1

## Purpose

Keep existing current save/simulate JSON usable while moving persistence to the framework-neutral schema.

## Scope

- In: Import/export parsing, localStorage migration, v1 `SavedDynamicsSystem` compatibility, built-in preset migration.
- Out: Browser UI redesign.

## Touchpoints

- Create:
  - `src/games/foundation/dynamics/FoundationDynamicsStorage.ts`
- Modify:
  - `src/games/foundation/dynamics/FoundationDynamicsModel.ts`
  - `tests/games/foundation/dynamics/FoundationDynamicsModel.test.ts`

## Acceptance Criteria

- [x] Current v1 saved JSON imports successfully.
- [x] Exported JSON uses the new versioned schema.
- [x] Invalid JSON returns clear errors.
- [x] Built-in systems load through the same schema path as saved systems.

## Test Plan

- `migration_imports_v1_saved_system` - preserves existing localStorage/import shape.
- `export_uses_canonical_schema_version` - proves new output format.

## Working Notes

- Planning baseline created.
- Added `FoundationDynamicsStorage.ts` with v1 compatibility import and v2 schema export.
- Kept existing model-level load/save/import/export functions stable for the page.
- Passed `npx vitest run tests/games/foundation/dynamics/FoundationDynamicsModel.test.ts tests/games/foundation/dynamics/FoundationDynamicsSchema.test.ts`.
