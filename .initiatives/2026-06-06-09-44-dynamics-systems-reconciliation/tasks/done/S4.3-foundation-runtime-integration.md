# S4.3: Foundation Runtime Integration

**Status**: DONE
**Wave**: W4
**Track**: Runtime
**Supports**: D4
**Effort**: Large
**Dependencies**: S4.2

## Purpose

Make Foundation runtime food/population ticking use the compiled dynamics adapter instead of directly calling duplicate hand-written formula paths.

## Scope

- In: `advanceTick()` integration, snapshot metrics, update metrics, runtime tests.
- Out: Wilderness exploration behavior changes.

## Touchpoints

- Create:
- Modify:
  - `src/games/foundation/runtime/FoundationRuntime.ts`
  - `tests/games/foundation/runtime.test.ts`

## Acceptance Criteria

- [x] `advanceTick()` applies food stock and troop changes from compiled dynamics.
- [x] Runtime snapshot/update metrics remain behaviorally compatible.
- [x] Existing Foundation runtime tests pass or are updated to assert compiled-dynamics behavior.

## Test Plan

- `runtime_updates_population_from_compiled_dynamics` - proves live path.
- `runtime_emits_food_metrics_from_compiled_dynamics` - validates update payload metrics.

## Working Notes

- Replaced direct Foundation economy formula calls in `FoundationRuntime` with the compiled dynamics runtime adapter.
- `snapshot()` and update metrics now use graph-backed adapter metrics; wilderness exploration remains imperative.
- Added runtime coverage proving `advanceTick()` matches the compiled adapter output.
- Verification: `npx vitest run tests/games/foundation/runtime.test.ts tests/games/foundation/dynamics/FoundationDynamicsRuntimeAdapter.test.ts tests/games/foundation/dynamics/FoundationEconomyDynamics.test.ts` passed, 3 files / 32 tests.
