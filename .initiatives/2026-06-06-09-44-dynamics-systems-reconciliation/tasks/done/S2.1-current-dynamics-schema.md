# S2.1: Current Dynamics Schema

**Status**: DONE
**Wave**: W2
**Track**: Schema
**Supports**: D2
**Effort**: Medium
**Dependencies**: W1

## Purpose

Create a framework-neutral schema for the current save/simulate flow without changing the editor's simple `input`, `operator`, and `sink` mental model.

## Scope

- In: Versioned dynamics definition, scenario/defaults, view metadata, runtime state types, stable ids, and stable handles.
- Out: Old deleted schema resurrection.

## Touchpoints

- Create:
  - `src/games/foundation/dynamics/FoundationDynamicsSchema.ts`
  - `tests/games/foundation/dynamics/FoundationDynamicsSchema.test.ts`
- Modify:
  - `src/games/foundation/dynamics/FoundationDynamicsModel.ts`

## Acceptance Criteria

- [x] Schema types do not import `@xyflow/react`.
- [x] Definition data is separate from scenario values, view positions, and runtime traces.
- [x] Stable ids and handles remain the runtime binding surface.

## Test Plan

- `schema_separates_definition_scenario_view_state` - verifies canonical separation.
- `schema_rejects_duplicate_node_ids` - verifies stable identity validation.

## Working Notes

- Planning baseline created.
- Added `FoundationDynamicsSchema.ts` with framework-neutral definition, scenario, view, runtime state, and validation types.
- Updated `FoundationDynamicsModel.ts` to reuse schema primitive/input kind types.
- Passed `npx vitest run tests/games/foundation/dynamics/FoundationDynamicsSchema.test.ts tests/games/foundation/dynamics/FoundationDynamicsModel.test.ts`.
