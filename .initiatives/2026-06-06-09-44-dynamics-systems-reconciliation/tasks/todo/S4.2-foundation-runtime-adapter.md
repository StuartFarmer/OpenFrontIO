# S4.2: Foundation Runtime Adapter

**Status**: TODO
**Wave**: W4
**Track**: Adapter
**Supports**: D4
**Effort**: Large
**Dependencies**: S4.1

## Purpose

Create a typed adapter that maps Foundation runtime state into compiled graph inputs/sinks and maps compiled outputs back into runtime metrics and player updates.

## Scope

- In: Player/map/parameter reads, food stock sink state, troop sink state or population output, runtime metric extraction.
- Out: Direct UI changes.

## Touchpoints

- Create:
  - `src/games/foundation/runtime/FoundationDynamicsRuntimeAdapter.ts`
  - `tests/games/foundation/dynamics/FoundationDynamicsRuntimeAdapter.test.ts`
- Modify:
  - `src/games/foundation/runtime/FoundationRuntime.ts`

## Acceptance Criteria

- [ ] Adapter can evaluate current Foundation player state without mutating it.
- [ ] Adapter result contains all metrics currently emitted by `FoundationRuntime.snapshot()` and `advanceTick()`.
- [ ] Adapter parity tests pass against current hand-written functions.

## Test Plan

- `adapter_reads_foundation_player_state` - validates inputs and sink state mapping.
- `adapter_outputs_runtime_metrics` - validates metrics shape.
- `adapter_matches_current_formula_outputs` - parity against current domain functions.

## Working Notes

- Planning baseline created.
