# S2.2: V1 Persistence Migration

**Status**: TODO
**Wave**: W2
**Track**: Persistence
**Supports**: D2
**Effort**: Medium
**Dependencies**: S2.1

## Purpose

Keep existing current save/simulate JSON usable while moving persistence to the framework-neutral schema.

## Scope

- In: Import/export parsing, localStorage migration, v1 `SavedDynamicsSystem` compatibility, built-in preset migration.
- Out: Browser UI redesign.

## Touchpoints

- Create:
  - `src/games/foundation/dynamics/FoundationDynamicsStorage.ts`
- Modify:
  - `src/games/foundation/dynamics/FoundationDynamicsModel.ts`
  - `tests/games/foundation/dynamics/FoundationDynamicsModel.test.ts`

## Acceptance Criteria

- [ ] Current v1 saved JSON imports successfully.
- [ ] Exported JSON uses the new versioned schema.
- [ ] Invalid JSON returns clear errors.
- [ ] Built-in systems load through the same schema path as saved systems.

## Test Plan

- `migration_imports_v1_saved_system` - preserves existing localStorage/import shape.
- `export_uses_canonical_schema_version` - proves new output format.

## Working Notes

- Planning baseline created.
