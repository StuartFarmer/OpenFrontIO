# S2.1: Command And Update Envelopes

**Status**: TODO
**Wave**: W2
**Track**: Runtime
**Supports**: D3
**Effort**: Medium
**Dependencies**: W1

## Purpose
Define small Foundation-owned command and update envelopes that follow the parent generic protocol direction without modifying global OpenFront schemas.

## Scope
- In: Foundation command payloads, placement command, update envelope, map delta fields, module event fields, error/result types if needed.
- Out: Shared `src/core/Schemas.ts` migration, OpenFront bridge, multiplayer server protocol.

## Touchpoints
- Create: `src/games/foundation/runtime/FoundationProtocol.ts`
- Create: `src/games/foundation/runtime/FoundationCommands.ts`
- Modify: None expected outside Foundation.

## Acceptance Criteria
- [ ] Placement command is Foundation-owned and does not use OpenFront `Intent`.
- [ ] Update envelope separates map/tile updates from module events.
- [ ] Envelope fields preserve a future worker/multiplayer path: module id, tick, command payload, update payload, metrics if useful.
- [ ] Types are narrow enough for the MVP and do not predefine population/food commands.

## Test Plan
- `foundation_protocol_accepts_place_command` - validates typed command construction/parsing if runtime validation is added.
- `foundation_update_envelope_carries_tile_delta` - validates expected update shape for placement.

## Working Notes
- Parent target update shape is documented in `.initiatives/2026-05-29-13-44-engine-sdk-game-modules/REPORT.md`.
