# S2.3: Runtime Tests

**Status**: DONE
**Wave**: W2
**Track**: Runtime
**Supports**: D3, D6
**Effort**: Small
**Dependencies**: S2.1, S2.2

## Purpose

Lock the first Foundation runtime loop before client rendering is wired to it.

This ticket proves the W2 public behavior through focused Vitest coverage: command construction, dispatch, tile delta updates, repeated placement policy, and the absence of deferred population/food systems.

## Scope

- In: command/update tests, repeated placement policy, debug snapshot shape.
- Out: Renderer adapter tests, DOM route tests.
- Out: OpenFront protocol compatibility tests; W2 should assert Foundation does not use those shapes.

## Touchpoints

- Create: `tests/games/foundation/runtime.test.ts`
- Modify: Foundation runtime files only if tests expose missing contract details.

## Acceptance Criteria

- [x] Runtime tests run independently.
- [x] Tests verify no population/food ticking occurs in the MVP loop.
- [x] Tests verify command/update shape is not OpenFront `GameUpdateViewData`.
- [x] Tests verify changed tile refs and states are paired for renderer integration.
- [x] Tests verify debug snapshot exposes only current real runtime values.

## Test Plan

- `npx vitest run tests/games/foundation/runtime.test.ts` - validates W2 behavior.
- `npx tsc --noEmit` - validates runtime export and type shape if feasible.

## Working Notes

- Planning baseline created.
- Hardened for Batch 3 W2 runtime tests.
- Moved to `doing` for implementation.
- Added `tests/games/foundation/runtime.test.ts` with command, dispatch, delta, repeated-placement, and no population/food coverage.
- Verified by `npx vitest run tests/games/foundation/runtime.test.ts`, all Foundation tests, and `npx tsc --noEmit`.
