# S1.4: Domain And Map Tests

**Status**: DONE
**Wave**: W1
**Track**: Join
**Supports**: D2, D6
**Effort**: Small
**Dependencies**: S1.2, S1.3

## Purpose

Add focused Vitest coverage for Foundation map and placement behavior before runtime/client code depends on it.

## Scope

- In: unit tests for map generation, addressing, terrain query adapter, fixed-radius placement, owner state updates.
- Out: WebGL rendering, route/component tests, runtime command tests.

## Touchpoints

- Create: `tests/games/foundation/map.test.ts`
- Create: `tests/games/foundation/placement.test.ts`
- Modify: Test utilities only if necessary.

## Acceptance Criteria

- [x] Tests cover semantic-free map API.
- [x] Tests cover module-owned terrain query behavior.
- [x] Tests cover fixed-radius placement and edge clipping.
- [x] Tests can run independently with `npx vitest run tests/games/foundation/map.test.ts tests/games/foundation/placement.test.ts`.

## Test Plan

- `npx vitest run tests/games/foundation/map.test.ts tests/games/foundation/placement.test.ts` - validates W1 behavior.

## Working Notes

- Planning baseline created.
- Completed: added `tests/games/foundation/map.test.ts` and `tests/games/foundation/placement.test.ts`.
- Verification: `npx vitest run tests/games/foundation/map.test.ts tests/games/foundation/placement.test.ts` passed with 7 tests.
- Verification: `npx tsc --noEmit` passed.
