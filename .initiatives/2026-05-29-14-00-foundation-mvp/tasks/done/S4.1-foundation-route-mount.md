# S4.1: Foundation Route Mount

**Status**: DONE
**Wave**: W4
**Track**: A - route/client mount
**Supports**: D5
**Effort**: Medium
**Dependencies**: W3

## Purpose
Expose Foundation through a dedicated client route that does not mount the OpenFront sandbox shell.

## Scope
- In: route detection, dynamic import of Foundation client component/module, minimal body shell, game surface cleanup.
- Out: OpenFront lobby integration, server matchmaking, multiplayer, OpenFront sandbox shell refactor.

## Touchpoints
- Create: `src/games/foundation/client/FoundationPage.ts`
- Modify: `src/client/Main.ts`
- Modify: Tests under `tests/client/` or `tests/games/foundation/` as needed.

## Acceptance Criteria
- [ ] A chosen route such as `/foundation` or `/sandbox/foundation` mounts Foundation.
- [ ] Route does not call `renderSandboxShell()`.
- [ ] Route removes existing game surfaces before mounting, consistent with current client cleanup expectations.
- [ ] Existing route behavior remains intact.

## Test Plan
- `foundation_route_mounts_without_openfront_shell` - validates route/body markup does not include OpenFront HUD elements.
- `npm run build-dev` - catches route import and type issues.

## Working Notes
- `src/client/Main.ts` is currently dirty in the worktree; implementation must read and preserve unrelated edits.
- `/foundation` and `/foundation.html` now dynamically mount `foundation-page` without `renderSandboxShell()`, after calling `removeExistingGameSurfaces()`.
