# S3.3: Render Runtime Integration

**Status**: DONE
**Wave**: W3
**Track**: Join
**Supports**: D4
**Effort**: Medium
**Dependencies**: S3.1, S3.2, W2

## Purpose
Connect Foundation runtime updates to the WebGL adapter so click placement visibly claims tiles.

## Scope
- In: client orchestrator, runtime command dispatch, update subscription/application, initial render, cleanup.
- Out: route registration, debug panel UI, population/food ticking.

## Touchpoints
- Create: `src/games/foundation/client/FoundationClient.ts`
- Modify: Foundation runtime/renderer adapter only as needed for a clean mount API.

## Acceptance Criteria
- [ ] Client mount initializes runtime and renderer.
- [ ] Click command updates runtime state and WebGL tile ownership.
- [ ] Initial render shows blank grass terrain before placement.
- [ ] Client cleanup disposes renderer and input adapter.

## Test Plan
- `foundation_client_applies_place_update_to_renderer` - validates runtime update is passed to renderer adapter.
- `foundation_client_disposes_adapters` - validates cleanup path.

## Working Notes
- This ticket is the first full vertical slice, but still not the route-visible product.
- `FoundationPage` now owns the local runtime, applies placement deltas to the shared WebGL adapter, and disposes renderer/input listeners on disconnect.
