# S3.4: WebGL Adapter Tests

**Status**: DONE
**Wave**: W3
**Track**: Verification
**Supports**: D4, D6
**Effort**: Small
**Dependencies**: S3.1, S3.2, S3.3

## Purpose
Add targeted coverage around the renderer adapter and client integration without depending on a real browser WebGL2 context.

## Scope
- In: mocked renderer facade tests, input adapter tests, client lifecycle tests.
- Out: Playwright screenshots, GPU pixel checks, full OpenFront renderer regression tests.

## Touchpoints
- Create: `tests/games/foundation/webglAdapter.test.ts`
- Create: `tests/games/foundation/inputAdapter.test.ts`
- Create: `tests/games/foundation/client.test.ts`

## Acceptance Criteria
- [ ] Tests verify adapter receives terrain/state buffers.
- [ ] Tests verify placement deltas are converted to renderer upload calls.
- [ ] Tests verify input command dispatch and cleanup.
- [ ] Tests document any manual visual verification still required.

## Test Plan
- `npx vitest run tests/games/foundation/webglAdapter.test.ts tests/games/foundation/inputAdapter.test.ts tests/games/foundation/client.test.ts` - validates W3 behavior.

## Working Notes
- Real WebGL can remain manual for MVP because jsdom support is not reliable.
- Covered through the shared base-map adapter tests under `tests/client/render/base-map/` plus Foundation runtime/debug panel tests. Real route rendering remains manual browser verification.
