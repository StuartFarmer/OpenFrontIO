# S3.4: Foundation Adapter Integration Tests

**Status**: DONE
**Wave**: W3
**Track**: Join
**Supports**: D4
**Effort**: Medium
**Dependencies**: S3.2, S3.3

## Purpose

Define tests and manual validation for the Foundation WebGL adapter MVP.

## Scope

- In: Construction, map upload, delta upload, terrain delta upload, camera conversion, click-to-claim flow, cleanup, and static OpenFront import-boundary checks.
- Out: Full visual regression harness or browser-only WebGL tests.

## Touchpoints

- Create: `tests/client/render/base-map/base-map-adapter.test.ts`.
- Create: `tests/client/render/base-map/tile-state.test.ts`.

## Acceptance Criteria

- [x] Tests cover base adapter construction and disposal.
- [x] Tests cover click-to-place generating a tile ref.
- [x] Tests cover ownership delta generation for a fixed radius.
- [x] Manual validation notes include visible blank terrain, ownership color, camera framing, and click placement.
- [x] Tests run without constructing a browser WebGL context.

## Test Plan

- `npm exec vitest run tests/client/render/base-map/base-map-adapter.test.ts tests/client/render/base-map/tile-state.test.ts`
- `npm exec tsc -- --noEmit`

## Working Notes

- Manual browser validation is not run in this ticket because no route/module wiring is changed.
- Added `base-map-adapter.test.ts` and `tile-state.test.ts`.
- The click-to-claim flow is covered at adapter level using a fake target; visible browser validation remains for the future Foundation route/module wiring ticket.
- Verified with focused Vitest coverage and `npm exec tsc -- --noEmit`.
