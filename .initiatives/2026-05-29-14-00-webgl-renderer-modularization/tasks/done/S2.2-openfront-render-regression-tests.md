# S2.2: OpenFront Render Regression Tests

**Status**: DONE
**Wave**: W2
**Track**: Sequential
**Supports**: D3
**Effort**: Medium
**Dependencies**: S2.1

## Purpose

Add or identify regression tests that protect OpenFront while renderer modularization proceeds.

## Scope

- In: Focused adapter type tests and frame upload dispatch tests that protect the existing OpenFront `FrameData` upload semantics.
- Out: Full browser screenshot automation, HUD smoke rewrites, and route/client mount changes.

## Touchpoints

- Create: `tests/client/render/base-map/openfront-compatibility.test.ts`.
- Modify: Existing tests only if imports move, which is not expected.

## Acceptance Criteria

- [x] Tests prove OpenFront frame upload still calls tile, trail, unit, structure, name, relation, and effect update paths as expected.
- [x] Tests prove adapter creation does not require Foundation-specific code.
- [x] Tests avoid brittle pixel assertions unless a later browser test harness is added.
- [x] Tests exercise live full upload, live delta upload, copy full upload, copy delta upload, and `skipTileUpload`.

## Test Plan

- `openfront-compatibility.test.ts` - focused Vitest coverage for upload dispatch and wrapper construction.

## Working Notes

- Browser visual parity is deferred; this ticket protects the non-pixel dispatch contract.
- Added `tests/client/render/base-map/openfront-compatibility.test.ts`.
- Verified OpenFront frame upload dispatch without browser pixel assertions.
