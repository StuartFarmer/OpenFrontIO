# S2.1: OpenFront Adapter Wrapper

**Status**: TODO
**Wave**: W2
**Track**: Sequential
**Supports**: D3
**Effort**: Medium
**Dependencies**: S1.1, S1.2, S1.3

## Purpose
Make the current OpenFront WebGL path explicit as an OpenFront adapter while preserving behavior.

## Scope
- In: Wrapping or naming `WebGLFrameBuilder`, `uploadFrameData()`, and `ClientGameRunner` WebGL setup as the OpenFront compatibility path.
- Out: Moving all renderer files to `src/games/openfront` or rewriting frame data.

## Touchpoints
- Create: Possible `OpenFrontWebGLAdapter` wrapper.
- Modify: `src/client/WebGLFrameBuilder.ts`, `src/client/ClientGameRunner.ts`, `src/client/render/frame/Upload.ts` only if implementation proceeds.

## Acceptance Criteria
- [ ] OpenFront still constructs the same WebGL canvas and renderer.
- [ ] `WebGLFrameBuilder.update(gameView)` still syncs players, local player, spawn overlay, terrain deltas, and full frame upload.
- [ ] Any new wrapper makes clear that OpenFront frame data is not the base renderer contract.

## Test Plan
- `npm run build-dev` - catches import/type regressions.
- `npm test` - catches current client/view/input behavior changes.

## Working Notes
- Planning baseline created.
