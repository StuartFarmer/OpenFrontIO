# S2.2: OpenFront Render Regression Tests

**Status**: TODO
**Wave**: W2
**Track**: Sequential
**Supports**: D3
**Effort**: Medium
**Dependencies**: S2.1

## Purpose
Add or identify regression tests that protect OpenFront while renderer modularization proceeds.

## Scope
- In: Adapter type tests, frame upload dispatch tests, existing `GameView.frameData()` tests, and InputHandler/HUD smoke coverage.
- Out: Full browser screenshot automation unless required by implementation risk.

## Touchpoints
- Create: New tests under `tests/client/render/` if missing.
- Modify: Existing tests only to align imports after wrapper creation.

## Acceptance Criteria
- [ ] Tests prove OpenFront frame upload still calls tile, trail, unit, structure, name, relation, and effect update paths as expected.
- [ ] Tests prove adapter creation does not require Foundation-specific code.
- [ ] Tests avoid brittle pixel assertions unless a later browser test harness is added.

## Test Plan
- `test_openfront_frame_adapter_uploads_existing_frame_data` - intent for dispatch compatibility.
- `test_openfront_webgl_adapter_preserves_public_builder_behavior` - intent for wrapper behavior.

## Working Notes
- Planning baseline created.
