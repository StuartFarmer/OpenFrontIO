# S2.3: Generator Tests And Check Mode

**Status**: TODO
**Wave**: W2
**Track**: Validation
**Supports**: D2, D5
**Effort**: Medium
**Dependencies**: S2.1

## Purpose

Make generated docs safe to maintain by testing generator behavior and adding a drift-check command.

## Scope
- In: Unit tests for generator formatting; `docs:check` command that fails on stale generated output.
- Out: GitHub Actions wiring, broad mechanics coverage.

## Touchpoints
- Create: generator tests under `tests/` or `scripts/__tests__/`
- Modify: `package.json`
- Modify: generator CLI from S2.2 if check mode is integrated there

## Acceptance Criteria
- [ ] `npm run docs:check` detects stale generated files.
- [ ] Tests cover stable ordering, units, descriptions, formulas, source links, and bigint display.
- [ ] Check mode does not rewrite files when run in CI mode.

## Test Plan
- `npm run docs:check` - verifies generated output is fresh.
- `npx vitest run <generator-test-file>` - verifies generator behavior.

## Working Notes
- Planning baseline created.
