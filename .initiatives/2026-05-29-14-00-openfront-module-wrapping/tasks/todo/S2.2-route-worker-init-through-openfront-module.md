# S2.2: Route Worker Init Through OpenFront Module

**Status**: TODO
**Wave**: W2
**Track**: Worker
**Supports**: D2
**Effort**: M
**Dependencies**: S2.1

## Purpose
Make the worker initialize the OpenFront runner through the module boundary while keeping current worker messages and transferable update batching.

## Scope
- In: `init` message runner creation, worker module lookup, OpenFront update/error callback, existing batch transfer handling.
- Out: Generic worker message envelopes, removing OpenFront RPCs.

## Touchpoints
- Modify: `src/core/worker/Worker.worker.ts`
- Potentially Modify: `src/core/worker/WorkerClient.ts`
- Potentially Modify: `src/core/worker/WorkerMessages.ts` only for type imports or compatibility aliases.

## Acceptance Criteria
- [ ] Worker `init` uses OpenFront module by default.
- [ ] `game_update_batch` still transfers `packedTileUpdates` and `packedMotionPlans`.
- [ ] Existing `player_actions`, `player_buildables`, `player_profile`, `player_border_tiles`, `attack_clustered_positions`, and `transport_ship_spawn` RPCs still work for OpenFront.

## Test Plan
- `worker_client_update_parity` - Existing parity test remains green.
- `worker_openfront_default_module` - Worker init defaults to OpenFront for existing game starts.

## Working Notes
- Planning baseline created.
