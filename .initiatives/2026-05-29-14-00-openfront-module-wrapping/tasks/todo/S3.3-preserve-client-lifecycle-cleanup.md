# S3.3: Preserve Client Lifecycle Cleanup

**Status**: TODO
**Wave**: W3
**Track**: Client Lifecycle
**Supports**: D3, D5
**Effort**: M
**Dependencies**: S3.1

## Purpose
Ensure module wrapping does not regress cleanup, reconnect, stop, replay, save-game, or WebGL disposal behavior.

## Scope
- In: `ClientGameRunner.stop()`, worker cleanup, transport leave, DOM cleanup, WebGL disposal, sound disposal, intervals/timeouts.
- Out: Redesigning transport reconnect semantics.

## Touchpoints
- Modify: `src/client/ClientGameRunner.ts`
- Potentially Modify: `src/games/openfront/client/OpenFrontClientModule.ts`
- Potentially Create: lifecycle tests.

## Acceptance Criteria
- [ ] Stopping an OpenFront game disposes worker, transport, sound, WebGL, input overlay, and intervals as before.
- [ ] `shouldPreventWindowClose()` remains based on OpenFront active player state.
- [ ] Save-game behavior for OpenFront win updates remains unchanged.

## Test Plan
- `ClientGameRunner lifecycle test` - Verifies cleanup callback is invoked through module wrapper.
- `GameView.test.ts` - Existing update/view behavior remains green.

## Working Notes
- Planning baseline created.
