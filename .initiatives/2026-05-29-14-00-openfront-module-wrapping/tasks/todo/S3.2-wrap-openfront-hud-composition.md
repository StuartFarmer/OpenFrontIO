# S3.2: Wrap OpenFront HUD Composition

**Status**: TODO
**Wave**: W3
**Track**: HUD
**Supports**: D3
**Effort**: M
**Dependencies**: S3.1

## Purpose
Make the current HUD/controller composition explicitly OpenFront-owned without changing the composition.

## Scope
- In: `createRenderer()` placement/naming, OpenFront HUD custom element wiring, OpenFront controller list.
- Out: Feature toggles, optional HUD matrix, Foundation HUD.

## Touchpoints
- Potentially Create: `src/games/openfront/client/OpenFrontHud.ts`
- Potentially Modify: `src/client/hud/GameRenderer.ts`
- Potentially Keep: compatibility export from old path.

## Acceptance Criteria
- [ ] OpenFront HUD composition still wires all current elements and controllers.
- [ ] The module boundary makes clear this HUD is OpenFront-specific.
- [ ] No feature flags are introduced to hide OpenFront HUD pieces for other games.

## Test Plan
- `ControlPanel.test.ts` - Existing HUD integration guard.
- `WarshipSelectionController.test.ts` and `BuildPreviewController.test.ts` - Existing controller behavior guards.

## Working Notes
- Planning baseline created.
