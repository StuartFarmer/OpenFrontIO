# S2.1: Wrap OpenFront Game Runner

**Status**: TODO
**Wave**: W2
**Track**: Server
**Supports**: D2
**Effort**: L
**Dependencies**: S1.1, S1.2

## Purpose
Move current OpenFront server startup ownership behind `OpenFrontServerModule` without changing runner behavior.

## Scope
- In: Current `createGameRunner()` logic, `GameRunner` compatibility, OpenFront startup executions, current `Executor` use.
- Out: Rewriting `GameImpl`, generic world runtime, generic command router.

## Touchpoints
- Create: `src/games/openfront/server/OpenFrontServerModule.ts`
- Potentially Modify: `src/core/GameRunner.ts`
- Potentially Modify: import paths directly if code is moved.

## Acceptance Criteria
- [ ] OpenFront server module constructs `Config`, loads terrain, creates humans/nations, creates `Game`, and creates `Executor` as before.
- [ ] Spawn timer, random spawn, bots, nations, win check, and rail recompute behavior are preserved.
- [ ] Existing callers can still create an OpenFront runner.

## Test Plan
- `openfront_server_module_creates_runner` - Asserts module creates a runner for existing `GameStartInfo`.
- `openfront_startup_executions_preserved` - Verifies expected startup execution paths remain installed under representative configs.

## Working Notes
- Planning baseline created.
