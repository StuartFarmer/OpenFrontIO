# S3.1: Wrap Client Game Creation

**Status**: TODO
**Wave**: W3
**Track**: Client
**Supports**: D3
**Effort**: L
**Dependencies**: S1.1, S1.2

## Purpose
Move current `createClientGame()` ownership behind `OpenFrontClientModule.mount(ctx)` while preserving lobby flow.

## Scope
- In: Config creation, terrain loading, worker initialization, OpenFront `GameView`, input overlay creation, WebGL view creation, sound manager, frame loop, `ClientGameRunner`.
- Out: Generic client runtime, Foundation client, renderer refactor.

## Touchpoints
- Create: `src/games/openfront/client/OpenFrontClientModule.ts`
- Potentially Modify: `src/client/ClientGameRunner.ts`
- Potentially Create: compatibility wrappers for existing exported functions/classes.

## Acceptance Criteria
- [ ] `joinLobby()` still exposes the same result shape.
- [ ] Client game creation delegates through OpenFront module.
- [ ] Existing OpenFront start, replay, and single-player paths keep behavior.

## Test Plan
- `openfront_client_module_mounts_current_runner` - Asserts mount returns a runner-compatible runtime.
- `LocalServer.test.ts` - Existing local server/client flow remains green.

## Working Notes
- Planning baseline created.
