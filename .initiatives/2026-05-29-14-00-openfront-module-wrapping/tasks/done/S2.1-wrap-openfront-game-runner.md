# S2.1: Wrap OpenFront Game Runner

**Status**: DONE
**Wave**: W2
**Track**: Server
**Supports**: D2
**Effort**: L
**Dependencies**: S1.1, S1.2

## Purpose
Move current OpenFront server startup ownership behind `OpenFrontServerModule` without changing runner behavior.

## Scope
- In: Current `createGameRunner()` logic, `GameRunner` compatibility, OpenFront startup executions, current `Executor` use.
- Out: Rewriting `GameImpl`, generic world runtime, generic command router.

## Touchpoints
- Create: `src/games/openfront/server/OpenFrontServerModule.ts`
- Potentially Modify: `src/core/GameRunner.ts`
- Potentially Modify: import paths directly if code is moved.

## Acceptance Criteria
- [x] OpenFront server module constructs `Config`, loads terrain, creates humans/nations, creates `Game`, and creates `Executor` as before.
- [x] Spawn timer, random spawn, bots, nations, win check, and rail recompute behavior are preserved.
- [x] Existing callers can still create an OpenFront runner.

## Test Plan
- `openfront_server_module_creates_runner` - Asserts module creates a runner for existing `GameStartInfo`.
- `openfront_startup_executions_preserved` - Verifies expected startup execution paths remain installed under representative configs.

## Working Notes
- Planning baseline created.
- Added `src/games/openfront/server/OpenFrontServerModule.ts` as the OpenFront-owned server runtime wrapper.
- `src/core/GameRunner.ts` remains in place for compatibility; no file moves or re-export shims were introduced.
- Focused validation passed: `npx vitest run tests/games/openfront/OpenFrontServerModule.test.ts tests/core/systems/WorkerClientUpdateParity.test.ts tests/games/registry.test.ts`.
