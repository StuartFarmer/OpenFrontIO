# S1.2: Add OpenFront Registry Default

**Status**: DONE
**Wave**: W1
**Track**: Boundary
**Supports**: D1
**Effort**: S
**Dependencies**: S1.1

## Purpose
Add a registry/default lookup so existing games resolve to OpenFront through one boundary lookup.

## Scope
- In: Registry, default module id, defensive lookup error for unknown modules.
- Out: Adding any non-OpenFront module.

## Touchpoints
- Create: `src/games/registry.ts`
- Modify: existing creation/mount entry points only enough to resolve `openfront`.

## Acceptance Criteria
- [x] Missing module id resolves to OpenFront.
- [x] Unknown module id fails clearly at the boundary.
- [x] No gameplay files contain repeated `moduleId` switches.

## Test Plan
- `registry_defaults_to_openfront` - Asserts undefined module id returns OpenFront.
- `registry_rejects_unknown_module` - Asserts unknown id produces a clear error.

## Working Notes
- Planning baseline created.
- Added `src/games/registry.ts` with `DEFAULT_GAME_MODULE_ID`, `getGameModule`, `registerGameModule`, and `listGameModules`.
- Added `tests/games/registry.test.ts` covering default OpenFront resolution, unknown module errors, and OpenFront module shape.
- Verified with `npx vitest run tests/games/registry.test.ts`.
