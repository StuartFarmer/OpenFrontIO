# S3.2: Keybinds And Preview

**Status**: TODO
**Wave**: W3
**Track**: Input and preview
**Supports**: D3
**Effort**: M
**Dependencies**: W1

## Purpose

Make keyboard build selection and build previews match the visible build UI.

## Scope
- In: default keybinds, parsed keybind labels, input build resolver, build-preview behavior for Station/Silo, tests.
- Out: Settings modal redesign; removing legacy SAM/Missile keybind settings unless necessary.

## Touchpoints
- Create: None expected.
- Modify: `src/core/game/UserSettings.ts`, `src/client/InputHandler.ts`, `src/client/controllers/BuildPreviewController.ts`, input/preview tests.

## Acceptance Criteria
- [ ] Station and Silo can be selected through intended hotkeys.
- [ ] Missile Silo and SAM Launcher are not selected by ordinary build hotkeys if hidden from ordinary build UI.
- [ ] Build preview handles Station/Silo placement, ghost rails, and preservation behavior deliberately.
- [ ] Existing attack/nuke hotkeys are unaffected.

## Test Plan
- `npx vitest run tests/InputHandler.test.ts tests/client/controllers/BuildPreviewController.test.ts` - hotkey and preview behavior.
- `npm run build-dev` - typecheck input and preview controllers.

## Working Notes
- Planning baseline created.
