# S3.1: Build Menu And Unit Display

**Status**: DONE
**Wave**: W3
**Track**: UI build surfaces
**Supports**: D3
**Effort**: M
**Dependencies**: W1

## Purpose

Expose Station and Silo in ordinary player build UI while hiding SAM Launcher and Missile Silo from those surfaces.

## Scope
- In: build modal table, radial build wheel source table, bottom build strip, counts, icon references.
- Out: Removing SAM Launcher/Missile Silo from simulation; lobby disabled-unit settings; map badge renderer.

## Touchpoints
- Create: optional image aliases under `resources/images/` if preferred.
- Modify: `src/client/hud/layers/BuildMenu.ts`, `src/client/hud/layers/UnitDisplay.ts`, tests for radial/build display.

## Acceptance Criteria
- [x] `BuildMenu.buildTable` includes Station and Silo entries.
- [x] `BuildMenu.buildTable` no longer includes Missile Silo or SAM Launcher entries.
- [x] `UnitDisplay` shows Station and Silo and hides Missile Silo/SAM Launcher.
- [x] DOM UI uses `resources/icons/rail-icon.svg` and `resources/icons/silo-icon.svg` or image aliases.

## Test Plan
- `npx vitest run tests/client/graphics/RadialMenuElements.test.ts` - radial menu source table behavior.
- `npm run build-dev` - typecheck Lit templates and imports.

## Working Notes
- Planning baseline created.
- Started implementation.
- Done: build table, radial source, and bottom strip now expose Rail Station and Silo in place of Missile Silo/SAM Launcher.
