# S5.1. Send Resource Modal

## Deliverable

D6. HUD Modal And In-Game Overlay Migration

## Objective

Migrate `SendResourceModal` to shared modal/form/action primitives as the reference pattern for HUD modal conversion.

## Scope

Review and migrate:

- `src/client/hud/layers/SendResourceModal.ts`

## Acceptance Criteria

- Troop and gold donation behavior is unchanged.
- Capacity clamp, presets, slider behavior, summary, dead-target handling, Escape, Enter, close, and confirm events are preserved.
- Styling is composed from shared primitives instead of raw button/modal/range markup where possible.

## Validation

```bash
npx tsc --noEmit
npx vitest run tests/client/hud/SendResourceModal.test.ts tests/client/components/UiComponents.test.ts
```

## Status

Done.

## Notes

- Modal surface, close action, available pill, presets, alerts, and footer actions now compose shared `ui-*` primitives.
- The custom range remains local because it owns capacity markers and live tooltip behavior not yet represented by the generic range primitive.
- Added focused coverage for shared primitive usage and troop capacity clamp on confirm.
