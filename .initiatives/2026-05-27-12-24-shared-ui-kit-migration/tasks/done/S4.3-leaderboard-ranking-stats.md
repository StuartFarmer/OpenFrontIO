# S4.3. Leaderboard, Ranking, Stats, And Game Info

## Deliverable

D5. Settings, Clan, Leaderboard, Ranking, And Stats Migration

## Objective

Migrate leaderboard, ranking, stats, and game-info surfaces to shared table/stat primitives.

## Scope

Review and migrate:

- `src/client/components/LeaderboardModal.ts`
- `src/client/components/leaderboard/*`
- `src/client/components/baseComponents/ranking/*`
- `src/client/components/baseComponents/stats/*`
- `src/client/components/GameInfoModal.ts`

## Acceptance Criteria

- Sorting, pagination, tab selection, and row actions remain unchanged where present.
- Stat and table visuals come from shared primitives.
- Dense data remains readable at common desktop and mobile widths.

## Validation

```bash
npx tsc --noEmit
npx vitest run tests/client/LeaderboardModal.test.ts tests/client/components/UiComponents.test.ts
```

## Status

Done with validation caveat.

## Notes

- Leaderboard loading, empty, retry, and footer states now use shared UI primitives while preserving native table markup and header tooltips.
- Ranking controls now use `ui-button`.
- Player stats summary grid now uses `ui-stat-grid` and `ui-stat`.
- Game info loading/empty/game summary and game-list cards/actions now compose shared UI primitives.
- Focused validation passed:
  - `npx vitest run tests/client/LeaderboardModal.test.ts tests/client/components/UiComponents.test.ts`
- `npx tsc --noEmit` is currently blocked outside this ticket by untracked systems work:
  - `tests/core/systems/ProjectileSystem.test.ts(41,76): Argument of type '{}' is not assignable to parameter of type '{ targetTile?: number | undefined; trajectory: TrajectoryTile[]; }'.`
