# S2.3. Lobby Config And Map Components

## Deliverable

D3. Homepage, Game Start, And Lobby Migration

## Objective

Convert lobby configuration, lobby player display, and map selection surfaces to shared primitives.

## Scope

Review and migrate:

- `src/client/components/GameConfigSettings.ts`
- `src/client/components/LobbyConfigItem.ts`
- `src/client/components/LobbyPlayerView.ts`
- Map picker/display components identified in the inventory.

## Acceptance Criteria

- Lobby config values still bind and emit correctly.
- Map selection remains usable and visually stable.
- Lobby player rows preserve status and action behavior.

## Validation

```bash
npx tsc --noEmit
```

