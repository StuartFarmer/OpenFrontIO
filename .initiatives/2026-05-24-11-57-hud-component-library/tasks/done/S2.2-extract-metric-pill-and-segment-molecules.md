# S2.2: Extract Metric, Pill, And Segment Molecules

**Status**: DONE
**Wave**: W2
**Track**: Controls A
**Supports**: D2
**Effort**: M
**Dependencies**: S2.1

## Purpose

Promote metric bars, rate pills, gold/resource pills, and segmented tab buttons into reusable HUD molecules.

## Scope

- In: Metric display molecule, rate pill molecule, icon pill molecule, segmented metric option molecule.
- Out: Changing control panel metric selection behavior.

## Touchpoints

- Create: HUD UI molecule helpers if needed.
- Modify: `src/client/hud/ui/HudTheme.ts`, possible new files under `src/client/hud/ui`.

## Acceptance Criteria

- [x] Troop/resource segmented controls can be built without raw per-call Tailwind structure.
- [x] Pill variants inherit icon fill and numeric text behavior.
- [x] Metric meter text layout is reusable.

## Test Plan

- `npx tsc --noEmit` - verify helper signatures and consumers.
- `npx eslint src/client/hud/ui` - verify lint cleanliness.

## Working Notes

- Planning baseline created.
- Added `renderHudIconPill()` and `renderHudMeter()` in `HudControls.ts`.
- Kept segmented control classes in `HudMolecules.ts` for reuse without changing control-panel behavior yet.
- Updated `/hud-kit` control-panel molecule samples to use the shared icon pill and meter helpers.
- Verified with `npx tsc --noEmit` and `npx eslint src/client/hud/ui src/client/hud/demo/HudPanelWorkbench.ts`.
