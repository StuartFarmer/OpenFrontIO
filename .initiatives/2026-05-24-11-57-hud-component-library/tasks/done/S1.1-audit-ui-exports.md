# S1.1: Audit HUD UI Exports

**Status**: DONE
**Wave**: W1
**Track**: Boundary
**Supports**: D1
**Effort**: S
**Dependencies**: None

## Purpose

Create a precise map of current HUD UI exports and classify each as primitive, molecule, composite, or component-specific compatibility export.

## Scope

- In: `HudTheme.ts`, `LucideIcon.ts`, current imports from active HUD layers, and `/hud-kit` usage.
- Out: Non-HUD app components and gameplay behavior.

## Touchpoints

- Create: None expected.
- Modify: Planning notes or comments only if needed; prefer no code change unless useful for the next ticket.

## Acceptance Criteria

- [x] Current exports are classified.
- [x] Component-specific exports that should remain compatibility aliases are identified.

## Test Plan

- `npx tsc --noEmit` - confirm no accidental code breakage if files are touched.

## Working Notes

- Planning baseline created.
- Classified current exports into:
  - `HudPrimitives.ts`: typography, surfaces, table cells, buttons, inputs, range bases, icons, labels, and meters.
  - `HudMolecules.ts`: blend rows, segmented controls, control rows, pills, event rows, build strip items, tooltips, identity rows, and stat rows.
  - `HudComposites.ts`: specialized HUD layer structures that should remain compatibility exports for now, including attack rows, attack ratio pills, toolbar, and timer label.
- Existing active consumers import through `HudTheme.ts`, so compatibility remains intact.
- Verified with `npx tsc --noEmit` and `npx eslint src/client/hud/ui`.
