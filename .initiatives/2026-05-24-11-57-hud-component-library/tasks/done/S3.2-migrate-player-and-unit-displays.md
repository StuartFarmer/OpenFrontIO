# S3.2: Migrate Player And Unit Displays

**Status**: DONE
**Wave**: W3
**Track**: Display widgets
**Supports**: D3
**Effort**: M
**Dependencies**: S2.1, S2.3

## Purpose

Move player overlay meters/pills and unit display strip/tooltip structure onto shared HUD primitives and molecules.

## Scope

- In: `PlayerInfoOverlay` mini meter, identity/icon rows, unit display strip, unit item tooltip shell.
- Out: Player/unit data formatting changes.

## Touchpoints

- Create: None expected.
- Modify: `src/client/hud/layers/PlayerInfoOverlay.ts`, `src/client/hud/layers/UnitDisplay.ts`.

## Acceptance Criteria

- [x] Player mini troop meter uses a reusable meter molecule.
- [x] Unit display strip and tooltip use shared HUD shell/tooltip primitives where applicable.
- [x] Existing unit hotkey/icon/count layout remains intact.

## Test Plan

- `npx tsc --noEmit` - verify compile.
- `npx eslint src/client/hud/layers/PlayerInfoOverlay.ts src/client/hud/layers/UnitDisplay.ts` - verify lint.
- `/hud-kit` visual pass - verify player and unit panel demos.

## Working Notes

- Planning baseline created.
- Replaced the player troop mini bar shell with `renderHudMeter()` and
  `HUD_MINI_METER`.
- Replaced the unit display inner strip with `HUD_BUILD_STRIP`.
- Replaced the unit tooltip shell/title with `HUD_TOOLTIP` and
  `HUD_TOOLTIP_TITLE`.
- Preserved existing unit hotkey/icon/count markup and build hover behavior.
- Verified with `npx tsc --noEmit` and `npx eslint
  src/client/hud/layers/PlayerInfoOverlay.ts
  src/client/hud/layers/UnitDisplay.ts`.
