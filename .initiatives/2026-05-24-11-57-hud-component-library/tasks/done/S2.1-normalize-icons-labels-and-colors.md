# S2.1: Normalize Icons, Labels, And Semantic Colors

**Status**: DONE
**Wave**: W2
**Track**: Shared foundation
**Supports**: D2
**Effort**: M
**Dependencies**: S1.2

## Purpose

Make icon atoms, numeric labels, text labels, and semantic resource/status colors reusable across pills, meters, segmented controls, and blend sliders.

## Scope

- In: HUD icon atoms, Lucide icon helper usage, fixed-width numeric labels, resource/status color tokens.
- Out: Replacing game asset icons or changing resource naming.

## Touchpoints

- Create: Optional small color/icon helper file under `src/client/hud/ui`.
- Modify: `src/client/hud/ui/HudTheme.ts`, `src/client/hud/ui/LucideIcon.ts`.

## Acceptance Criteria

- [x] Resource/status colors are not duplicated across each molecule.
- [x] Icon atom sizing works for asset icons and Lucide icons.
- [x] Fixed-width HUD numeric labels are available for quantity displays.

## Test Plan

- `npx tsc --noEmit` - verify shared exports type-check.
- `npx eslint src/client/hud/ui` - verify lint cleanliness.

## Working Notes

- Planning baseline created.
- Added `HudColors.ts` with reusable resource fill/text/border classes and status tone classes.
- Added `renderHudMaskIcon()` so asset icons inherit the current text color consistently.
- Preserved existing fixed-width numeric label exports from `HudPrimitives.ts`.
- Verified with `npx tsc --noEmit` and `npx eslint src/client/hud/ui src/client/hud/demo/HudPanelWorkbench.ts`.
