# S3.1: Migrate Control Panel

**Status**: DONE
**Wave**: W3
**Track**: Control panel
**Supports**: D3
**Effort**: L
**Dependencies**: S2.2, S2.3

## Purpose

Replace control-panel-owned UI molecule markup with shared HUD molecules while preserving existing state and event behavior.

## Scope

- In: Metric tabs, metric meter, rate pill, gold pill, attack ratio control shell, resource blend slider.
- Out: Changing resource blend math, attack ratio behavior, or tick/update logic.

## Touchpoints

- Create: None expected.
- Modify: `src/client/hud/layers/ControlPanel.ts`.

## Acceptance Criteria

- [x] Control panel no longer owns the full blend dual range slider structure.
- [x] Control panel no longer owns the full metric meter text/separator/icon layout.
- [x] Existing compact and desktop layouts remain visually equivalent.

## Test Plan

- `npx tsc --noEmit` - verify compile.
- `npx eslint src/client/hud/layers/ControlPanel.ts` - verify lint.
- `/hud-kit` visual pass - verify control panel demo.

## Working Notes

- Planning baseline created.
- Replaced the control-panel metric meter shell with `renderHudMeter()`.
- Replaced rate and gold pill shells with `renderHudIconPill()`.
- Replaced local mask icon markup with `renderHudMaskIcon()`.
- Replaced the interactive blend dual range markup with `renderHudBlendSlider()`.
- Preserved existing attack ratio, resource blend, tick, and selected metric behavior.
- Verified with `npx tsc --noEmit` and `npx eslint src/client/hud/ui
  src/client/hud/layers/ControlPanel.ts`.
