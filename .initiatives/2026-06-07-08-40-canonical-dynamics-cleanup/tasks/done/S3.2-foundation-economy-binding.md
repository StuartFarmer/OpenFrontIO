# S3.2: Foundation Economy Binding

**Status**: DONE
**Wave**: W3
**Track**: Foundation binding
**Supports**: D3
**Effort**: Large
**Dependencies**: S3.1

## Purpose

Rework the Foundation economy runtime code from a one-off adapter into the
reference graph-backed binding/system implementation.

## Scope

- In: Foundation economy graph binding, metric extraction, player update logic,
  renamed tests.
- Out: Changing economy behavior.

## Touchpoints

- Create:
  - `src/games/foundation/runtime/FoundationEconomyDynamicsSystem.ts`
- Modify:
  - `src/games/foundation/runtime/FoundationDynamicsRuntimeAdapter.ts`
  - `src/games/foundation/runtime/index.ts`
  - `tests/games/foundation/dynamics/FoundationDynamicsRuntimeAdapter.test.ts`

## Acceptance Criteria

- [x] Foundation economy binding uses the generic graph binding contract.
- [x] Public runtime exports no longer present "adapter" as the long-term concept.
- [x] Existing adapter parity tests are renamed or replaced with binding tests.

## Test Plan

- `foundation_economy_binding_reads_player_state` - input/sink mapping remains
  correct.
- `foundation_economy_binding_outputs_metrics` - runtime metric shape remains
  compatible.

## Working Notes

- Added `FoundationEconomyDynamicsSystem` with a generic dynamics graph binding
  factory and economy snapshot/tick helpers.
- Converted `FoundationRuntime`, `FoundationCommandRouter`, runtime tests, and
  boundary tests to the new economy-system public names.
- Left `FoundationDynamicsRuntimeAdapter` as a compatibility wrapper only; it is
  no longer exported from the runtime barrel.
- Renamed adapter parity tests to `FoundationEconomyDynamicsSystem.test.ts` and
  added a direct generic binding contract test.
- Validation:
  - `npx eslint src/games/foundation/runtime/FoundationEconomyDynamicsSystem.ts src/games/foundation/runtime/FoundationDynamicsRuntimeAdapter.ts src/games/foundation/runtime/FoundationRuntime.ts src/games/foundation/runtime/FoundationCommandRouter.ts src/games/foundation/runtime/index.ts tests/games/foundation/dynamics/FoundationEconomyDynamicsSystem.test.ts tests/games/foundation/runtime.test.ts tests/core/systems/DynamicsGraphSystemBoundary.test.ts`
  - `npx vitest run tests/games/foundation/dynamics/FoundationEconomyDynamicsSystem.test.ts tests/games/foundation/runtime.test.ts tests/core/systems/DynamicsGraphSystemBoundary.test.ts tests/core/systems/DynamicsGraphBinding.test.ts`
