# S4.1: Remove Editor Fallback Simulation

**Status**: DONE
**Wave**: W4
**Track**: Editor fallback
**Supports**: D4
**Effort**: Medium
**Dependencies**: W2

## Purpose

Ensure normal editor simulation only runs through the canonical compiler and
simulator.

## Scope

- In: Remove or explicitly isolate fallback evaluator paths, diagnostics UX,
  page/model tests.
- Out: Draft-mode feature design unless needed to preserve current UX.

## Touchpoints

- Create:
- Modify:
  - `src/games/foundation/dynamics/FoundationDynamicsModel.ts`
  - `src/games/foundation/client/FoundationDynamicsPage.ts`
  - `tests/games/foundation/dynamics/FoundationDynamicsModel.test.ts`
  - `tests/games/foundation/client/FoundationDynamicsPage.test.ts`

## Acceptance Criteria

- [x] Invalid compiled graphs do not simulate through fallback code.
- [x] Diagnostics remain visible and actionable.
- [x] Valid graphs still simulate and chart correctly.

## Test Plan

- `invalid_graph_does_not_step` - invalid formulas disable or block simulation.
- `valid_graph_simulates_with_compiler` - valid graphs still produce traces.

## Working Notes

- Removed the editor model's raw fallback evaluator path.
- `initialSimulationState` and `stepSimulationState` now call the canonical
  dynamics simulator directly.
- Added a regression test proving invalid graphs report diagnostics and do not
  step through fallback simulation.
- Validation:
  - `npx eslint src/games/foundation/dynamics/FoundationDynamicsModel.ts src/games/foundation/client/FoundationDynamicsPage.ts tests/games/foundation/dynamics/FoundationDynamicsModel.test.ts tests/games/foundation/client/FoundationDynamicsPage.test.ts`
  - `npx vitest run tests/games/foundation/dynamics/FoundationDynamicsModel.test.ts tests/games/foundation/client/FoundationDynamicsPage.test.ts`
  - `rg -n "fallback|evaluateExpression|new Function|with \\(scope\\)|evaluateNodeValues|fallbackStepSimulationState|fallbackInitialSimulationState" src/games/foundation/dynamics/FoundationDynamicsModel.ts`
