# S3.3: Runtime Integration Renaming

**Status**: DONE
**Wave**: W3
**Track**: Runtime integration
**Supports**: D3
**Effort**: Medium
**Dependencies**: S3.2

## Purpose

Update Foundation runtime callers to use the graph-backed economy binding/system
names and remove transitional adapter imports.

## Scope

- In: Runtime imports, command router metrics, runtime tests.
- Out: Command/wilderness behavior changes.

## Touchpoints

- Create:
- Modify:
  - `src/games/foundation/runtime/FoundationRuntime.ts`
  - `src/games/foundation/runtime/FoundationCommandRouter.ts`
  - `src/games/foundation/runtime/index.ts`
  - `tests/games/foundation/runtime.test.ts`

## Acceptance Criteria

- [x] `FoundationRuntime` uses the graph-backed economy binding/system.
- [x] `FoundationCommandRouter` metrics still come from graph-backed economy.
- [x] No runtime imports reference `FoundationDynamicsRuntimeAdapter`.

## Test Plan

- `runtime_updates_economy_from_graph_binding` - live tick path remains graph
  backed.
- `command_router_metrics_from_graph_binding` - dispatch update metrics remain
  compatible.

## Working Notes

- Updated remaining runtime wording and tests to refer to the graph-backed
  economy system rather than the dynamics adapter.
- Verified no runtime imports reference `FoundationDynamicsRuntimeAdapter`; only
  the compatibility wrapper itself contains the legacy aliases.
- Validation:
  - `npx eslint src/games/foundation/runtime/FoundationRuntime.ts src/games/foundation/runtime/FoundationCommandRouter.ts src/games/foundation/runtime/index.ts src/games/foundation/domain/FoundationFood.ts src/games/foundation/domain/FoundationTroops.ts tests/games/foundation/runtime.test.ts`
  - `npx vitest run tests/games/foundation/runtime.test.ts tests/games/foundation/dynamics/FoundationEconomyDynamicsSystem.test.ts tests/core/systems/DynamicsGraphSystemBoundary.test.ts`
