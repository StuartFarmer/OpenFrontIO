# S2.2: Sandbox Run Side-Effect Bypass

**Status**: DONE
**Wave**: W2
**Track**: Lifecycle foundation
**Supports**: D3
**Effort**: M
**Dependencies**: S2.1

## Purpose

Ensure sandbox-launched runs do not participate in archive, achievement, or stats flows.

## Scope

- In: Sandbox run marker, local server/client runner side-effect checks, archive bypass, achievement/stat bypass relevant to sandbox runs.
- Out: Removing normal single-player archives/stats, server archive API changes beyond what sandbox bypass requires.

## Touchpoints

- Create: none expected.
- Modify: `src/client/LocalServer.ts`, `src/client/ClientGameRunner.ts`, `src/client/Transport.ts`, possibly `src/core/Schemas.ts` if the sandbox marker belongs in config.

## Acceptance Criteria

- [x] Sandbox runs are identifiable in local client lifecycle.
- [x] `LocalServer.endGame()` or equivalent archive path skips sandbox runs.
- [x] Achievement/stat submission paths are skipped or never entered for sandbox runs.
- [x] Normal single-player runs still archive and behave as before.

## Test Plan

- `tests/client/LocalServer.test.ts` - sandbox run does not archive.
- New client lifecycle test - normal single-player behavior remains unchanged.

## Working Notes

- 2026-05-25: Added `GameConfig.isSandbox`, skipped local persistent start/end records for sandbox lobbies, skipped single-player archive submission, and bypassed lobby URL/ad/stat hooks for sandbox joins.
- 2026-05-25: Added `tests/client/LocalServer.test.ts` coverage proving sandbox local server end does not fetch archive submission.
