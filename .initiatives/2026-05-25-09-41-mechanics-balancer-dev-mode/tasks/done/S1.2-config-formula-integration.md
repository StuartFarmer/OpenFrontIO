# S1.2: Config Formula Integration

**Status**: DONE
**Wave**: W1
**Track**: Core contract
**Supports**: D2
**Effort**: L
**Dependencies**: S1.1

## Purpose

Make production `Config` read resource and troop mechanics from the new mechanics contract while preserving default behavior.

## Scope

- In: Resource capacity, passive resource regen, terrain resource split, biomass-supported troop capacity, effective troop capacity, troop growth, bot/nation multipliers already in scope.
- Out: Live mutation, AI strategy changes, private/public lobby exposure, resource cost redesign.

## Touchpoints

- Create: none expected beyond S1.1.
- Modify: `src/core/configuration/Config.ts`, possibly `src/core/game/Resources.ts` if pure helper extraction is needed.

## Acceptance Criteria

- [x] Default `Config` behavior matches current formula outputs.
- [x] Custom troop growth rate changes `troopIncreaseRate(...)`.
- [x] Custom biomass baseline changes biomass-supported troop capacity.
- [x] Custom terrain weights change resource production split.
- [x] Formula reads use the defaulted mechanics contract, not scattered optional checks.

## Test Plan

- `tests/core/configuration/ResourceCapacity.test.ts` - default and custom formula assertions.
- `tests/core/executions/PlayerExecution.test.ts` - custom mechanics affect tick execution.

## Working Notes

- Planning baseline created.
- 2026-05-25: Started after S1.1. Initial formula wiring routes `Config` through resolved mechanics defaults for troop growth, biomass support, resource capacity, terrain weights, and resource regen.
- 2026-05-25: Extended `resourceRegenAmount` / `resourceRegenDelta` with optional formula parameters while preserving default call sites.
- 2026-05-25: Verified with `npx vitest run tests/core/configuration/ResourceCapacity.test.ts tests/core/executions/PlayerExecution.test.ts` and `npx tsc --noEmit`; both passed.
