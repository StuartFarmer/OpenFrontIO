# S2.3: Sandbox Route Shell

**Status**: DONE
**Wave**: W2
**Track**: Route shell
**Supports**: D3
**Effort**: S
**Dependencies**: None after analysis; can run alongside S2.1

## Purpose

Add the standalone `/sandbox` route shell following the repo's existing developer-route bootstrap pattern.

## Scope

- In: Route detection, component import/registration, standalone shell render, no normal nav exposure.
- Out: Final controls, final lifecycle wiring, production nav/menu entry.

## Touchpoints

- Create: `src/client/sandbox/SandboxBalancer.ts`
- Modify: `src/client/Main.ts`

## Acceptance Criteria

- [x] Visiting `/sandbox` renders the sandbox shell.
- [x] Normal app bootstrap remains unchanged for ordinary routes.
- [x] The route is not added to production navigation.
- [x] Shell layout can host controls and game lifecycle wiring in W3.

## Test Plan

- New client route/component test - sandbox shell custom element renders.
- `npx tsc --noEmit` - route imports type-check.

## Working Notes

- 2026-05-25: Added `src/client/sandbox/SandboxBalancer.ts` using HUD UI primitives and wired `/sandbox` into the developer-route bootstrap.
- 2026-05-25: Shell can dispatch a real sandbox `join-lobby` run with default mechanics, World map, zero bots, and nations disabled; W3 will replace the minimal launch surface with full balancer controls.
