# S4.2: Client Sandbox Tests

**Status**: TODO
**Wave**: W4
**Track**: Client validation
**Supports**: D5
**Effort**: M
**Dependencies**: W2, W3

## Purpose

Validate the sandbox route, JSON UX, pending/active state, HUD-kit usage, and lifecycle side-effect bypass with targeted client tests.

## Scope
- In: Sandbox component tests, local server archive bypass tests, config construction tests, HUD custom element registration/import coverage if new primitives are added.
- Out: Full visual regression suite, Playwright unless added deliberately later.

## Touchpoints
- Create: tests under `tests/client/sandbox/` if helpful.
- Modify: `tests/client/LocalServer.test.ts`, `tests/client/JoinLobbyModal.test.ts` or extracted builder tests.

## Acceptance Criteria
- [ ] `/sandbox` route/component test passes.
- [ ] JSON export/import tests pass.
- [ ] Pending values apply on restart only.
- [ ] Sandbox archive/achievement/stat bypass is tested.
- [ ] Any new HUD primitive used by the sandbox is registered and cataloged.

## Test Plan
- `npx vitest run tests/client/LocalServer.test.ts`
- `npx vitest run tests/client/JoinLobbyModal.test.ts`
- `npx vitest run tests/client/sandbox`

## Working Notes
- Planning baseline created.
