# S1.1: Mechanics Defaults And Schema

**Status**: TODO
**Wave**: W1
**Track**: Core contract
**Supports**: D1
**Effort**: M
**Dependencies**: None

## Purpose

Create the typed, defaulted, schema-validated mechanics contract for the first biomass/resource/troop tuning slice.

## Scope
- In: Default mechanics values, optional schema fields, validation/normalization helpers, version field if useful.
- Out: UI controls, runtime launch flow, private-lobby sharing, whole-game scripting.

## Touchpoints
- Create: `src/core/configuration/MechanicsConfig.ts`
- Modify: `src/core/Schemas.ts`

## Acceptance Criteria
- [ ] Current hard-coded biomass/resource/troop constants have named defaults in one mechanics module.
- [ ] `GameConfigSchema` accepts an optional mechanics section.
- [ ] Partial mechanics input merges with defaults deterministically.
- [ ] Invalid mechanic values are rejected by Zod.

## Test Plan
- `tests/core/configuration/ResourceCapacity.test.ts` - add/default-preservation schema checks or companion schema tests.
- `tests/StatsSchema.test.ts` or a new schema-focused test - verify valid and invalid mechanics blobs.

## Working Notes
- Planning baseline created.
