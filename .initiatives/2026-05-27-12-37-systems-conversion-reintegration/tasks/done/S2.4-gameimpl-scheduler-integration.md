# S2.4: GameImpl Scheduler Integration

**Status**: DONE
**Wave**: W2
**Track**: Validation
**Supports**: D2
**Effort**: L
**Dependencies**: S2.1, S2.2, S2.3

## Purpose

Integrate the systems scheduler into `GameImpl.executeNextTick()` while
preserving current behavior.

## Scope

- In: Scheduler construction, tick invocation, compatibility with updates,
  water ticking, hash emission, and pending legacy executions.
- Out: Native gameplay migrations.

## Touchpoints

- Modify: `src/core/game/GameImpl.ts`
- Modify: `tests/core/game/GameImpl.test.ts`, W1 parity fixtures

## Acceptance Criteria

- [x] Existing `GameImpl` tests pass.
- [x] W1 parity fixtures show no behavior drift.
- [x] Public game APIs remain source-compatible.

## Test Plan

- `gameimpl_scheduler_preserves_legacy_tick` - validates equivalent snapshots.
- `gameimpl_scheduler_preserves_updates` - validates update cadence.

## Working Notes

- Planning baseline created.
- Integrated `GameSystemScheduler` into `GameImpl.executeNextTick()`.
- Routed `addExecution`, `removeExecution`, and `executions` through
  `LegacyExecutionSystem`.
- Verified with focused systems, GameImpl, PlayerExecution, Attack, and W1
  parity tests plus `npx tsc --noEmit`.
