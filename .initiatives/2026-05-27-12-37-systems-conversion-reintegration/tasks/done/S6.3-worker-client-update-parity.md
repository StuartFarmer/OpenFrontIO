# S6.3: Worker Client Update Parity

**Status**: DONE
**Wave**: W6
**Track**: B
**Supports**: D6
**Effort**: M
**Dependencies**: W3, W4, W5

## Purpose

Prove worker-facing and client-facing updates remain equivalent after systems
migration.

## Scope

- In: `GameRunner` payload shape, `GameUpdates`, packed tile updates, motion
  plans, player update diffing, unit updates, attack updates, display events.
- Out: Client UI redesign.

## Touchpoints

- Modify: `src/core/GameRunner.ts`, `src/core/game/GameUpdates.ts`,
  `src/core/game/GameUpdateUtils.ts`, client view tests as needed.

## Acceptance Criteria

- [ ] Update snapshots match legacy fixtures.
- [ ] Client merge behavior remains compatible.

## Test Plan

- `worker_update_parity_scripted_game` - compares `GameUpdateViewData`.
- `client_state_merge_preserves_system_updates` - validates update merge.

## Working Notes

- Planning baseline created.
- Starting parity audit of worker/client update payload generation against the
  systems scheduler path.
- Added worker/client parity tests for `GameRunner` payload shape and partial
  client state merge compatibility.
- Validated with worker/client update tests, `GameUpdateUtils`, `GameView`, core
  game tests, and `npx tsc --noEmit`.
