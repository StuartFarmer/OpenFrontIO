# S1.4: High-Risk Scenario Fixtures

**Status**: DONE
**Wave**: W1
**Track**: B
**Supports**: D1
**Effort**: L
**Dependencies**: S1.1, S1.2, S1.3

## Purpose

Implement the first reusable parity fixtures for the highest-risk migration
surfaces.

## Scope

- In: Fixtures for player economy, active attack, counterattack, conquest,
  warship behavior, transport landing, nuke/SAM interaction, and update payload
  stability.
- Out: Native system replacement.

## Touchpoints

- Create: `tests/core/systems/SystemsParity.test.ts`
- Modify: `tests/util/Setup.ts` only if setup reuse requires it.

## Acceptance Criteria

- [x] Fixtures pass against the legacy path.
- [x] Fixtures are reusable by later waves when systems paths are introduced.

## Test Plan

- `parity_legacy_economy_fixture` - baseline economy snapshot.
- `parity_legacy_attack_fixture` - baseline attack and tile capture snapshot.
- `parity_legacy_unit_fixture` - baseline unit/projectile snapshot.

## Working Notes

- Planning baseline created.
- Started after S1.1, S1.2, and S1.3 completed.
- Added `tests/core/systems/SystemsParity.test.ts` with baseline parity
  fixtures for economy, attack, counterattack, conquest, unit, and update
  payload capture paths.
- Verified with `./node_modules/.bin/vitest run tests/core/systems/ParitySnapshot.test.ts tests/core/systems/ParityRunner.test.ts tests/core/systems/SystemsParity.test.ts`.
- Verified type safety with `npx tsc --noEmit`.
