# S3.3: Client Capacity Tests

**Status**: DONE
**Wave**: W3
**Track**: Join
**Supports**: D4, D5
**Effort**: Small
**Dependencies**: S3.1, S3.2

## Purpose

Add final focused tests that prove capacity propagation and UI display work together.

## Scope
- In: Client state, view accessors, and control panel smoke tests.
- Out: Browser/e2e tests.

## Context and Rationale
- S3.1 and S3.2 added the propagation and display behavior; this ticket is the join check that they work as a single client-facing slice.
- Existing focused tests are enough for this wave because the UI display is pure `ControlPanel.tick()` state plus Lit rendering.
- Browser/manual verification belongs to W4; this ticket stops at automated client tests.

## Touchpoints
- Create: None expected.
- Modify: `tests/GameUpdateUtils.test.ts`, `tests/client/view/PlayerView.test.ts`, `tests/client/hud/ControlPanel.test.ts`.

## Acceptance Criteria
- [x] Capacity diffs are emitted only when capacity values change.
- [x] Applying partial updates updates resource capacity state.
- [x] `PlayerView.resourceCapacity()` returns expected values.
- [x] Control panel test covers current/max rendering.
- [x] TypeScript accepts the combined client state contract.

## Test Plan
- `npx vitest run tests/GameUpdateUtils.test.ts tests/client/view/PlayerView.test.ts tests/client/hud/ControlPanel.test.ts` - verifies client capacity behavior.

## Working Notes
- Planning baseline created.
- Hardened 2026-05-21.
- `npx prettier --write ...` ran on the touched resource/capacity files.
- `npx vitest run tests/GameUpdateUtils.test.ts tests/client/view/PlayerView.test.ts tests/client/hud/ControlPanel.test.ts` passed with 45 tests.
- `npx vitest run tests/core/configuration/ResourceCapacity.test.ts tests/core/executions/PlayerExecution.test.ts tests/core/game/Resources.test.ts` passed with 20 tests after formatting.
- `npx tsc --noEmit` passed.
