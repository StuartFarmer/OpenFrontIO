# S5.3: Winner Archive Records

**Status**: TODO
**Wave**: W5
**Track**: C
**Supports**: D5
**Effort**: L
**Dependencies**: S3.1

## Purpose
Move winner/end-state and archive behavior behind module-owned record schemas so Foundation is not forced to implement OpenFront stats.

## Scope
- In: Service hooks and schemas for winner/end-state, stats payload, archive payload, and replay record support.
- In: OpenFront compatibility with current `WinnerSchema`, `AllPlayersStatsSchema`, and `PlayerRecord`.
- In: Foundation minimal records for local MVP.
- Out: Migrating existing archived records.

## Touchpoints
- Modify: `src/core/Schemas.ts` additively or create generic record schemas.
- Modify: `src/server/GameServer.ts`.
- Modify: `src/server/Worker.ts` single-player archive route only if generic records are introduced.
- Modify: `src/client/LocalServer.ts`.

## Acceptance Criteria
- [ ] OpenFront archives still validate and save.
- [ ] Foundation local runs can end with minimal winner/stats/archive payloads.
- [ ] Generic record formats are versioned before persistence.

## Test Plan
- `openfront_archive_payload_unchanged` - compatibility guard.
- `foundation_local_end_writes_minimal_archive_payload` - module guard.

## Working Notes
- Planning baseline created.
