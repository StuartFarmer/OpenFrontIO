# S4.3: Generic Update Batch Transfer

**Status**: TODO
**Wave**: W4
**Track**: Main
**Supports**: D4
**Effort**: M
**Dependencies**: S4.1

## Purpose
Allow worker update batching to transfer generic engine/module update envelopes, while OpenFront continues to bridge `GameUpdateViewData`.

## Scope
- In: Generic update batch message shape.
- In: Transferable buffer handling for generic map/tile update buffers.
- In: OpenFront bridge from `GameUpdateViewData` to generic batch or legacy batch during migration.
- Out: Full OpenFront renderer migration to generic updates.

## Touchpoints
- Modify: `src/core/worker/Worker.worker.ts`.
- Modify: `src/core/worker/WorkerMessages.ts`.
- Modify: `src/core/worker/WorkerClient.ts`.
- Modify: `tests/core/systems/WorkerClientUpdateParity.test.ts` or add a focused protocol test.

## Acceptance Criteria
- [ ] Generic update batches can include transferable typed-array buffers.
- [ ] OpenFront packed tile and motion-plan buffers still transfer correctly.
- [ ] Foundation can emit terrain/ownership updates without `GameUpdateViewData`.

## Test Plan
- `generic_update_batch_transfers_tile_state_buffer` - transfer guard.
- `openfront_worker_update_parity_unchanged` - existing parity guard.

## Working Notes
- Planning baseline created.
