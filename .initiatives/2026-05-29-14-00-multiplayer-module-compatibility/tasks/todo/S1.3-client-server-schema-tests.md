# S1.3: Client Server Schema Tests

**Status**: TODO
**Wave**: W1
**Track**: A
**Supports**: D1
**Effort**: S
**Dependencies**: S1.1

## Purpose
Protect existing OpenFront client/server message parsing while generic module envelopes are introduced.

## Scope
- In: Vitest coverage for old and new client/server message schema paths.
- In: Invalid payload rejection for envelope structure.
- Out: End-to-end WebSocket tests.

## Touchpoints
- Create: `tests/core/protocol/ModuleProtocol.test.ts`.
- Modify: Existing schema tests only if there is a local convention to extend.

## Acceptance Criteria
- [ ] Existing `ClientMessageSchema` samples still parse.
- [ ] Existing `ServerMessageSchema` samples still parse.
- [ ] Generic module messages fail when envelope fields are invalid.

## Test Plan
- `legacy_openfront_client_messages_still_parse` - compatibility guard.
- `generic_module_envelope_rejects_missing_module_id_when_not_legacy` - envelope guard.

## Working Notes
- Planning baseline created.
