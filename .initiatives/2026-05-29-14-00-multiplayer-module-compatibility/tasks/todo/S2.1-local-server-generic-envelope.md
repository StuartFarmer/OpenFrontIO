# S2.1: Local Server Generic Envelope

**Status**: TODO
**Wave**: W2
**Track**: Main
**Supports**: D2
**Effort**: M
**Dependencies**: S1.1, S1.2

## Purpose
Adapt `LocalServer` so single-player modules can use the same generic start/intent/turn lifecycle expected by remote multiplayer.

## Scope
- In: Local start messages, intent collection, turn creation, pause handling, rejoin handling, and `turnComplete()` alignment.
- In: OpenFront compatibility through adapters.
- Out: Remote WebSocket support for Foundation.

## Touchpoints
- Modify: `src/client/LocalServer.ts`.
- Modify: `src/client/Transport.ts` only where local message typing requires it.
- Create: Local protocol tests if existing `tests/client/LocalServer.test.ts` becomes too broad.

## Acceptance Criteria
- [ ] Existing OpenFront local games and replays continue to pass.
- [ ] A generic module payload can be collected into a local turn without OpenFront intent parsing.
- [ ] `turnComplete()` backlog behavior remains unchanged.

## Test Plan
- `local_server_queues_generic_module_turns` - validates Foundation-compatible path.
- `local_server_openfront_turn_pacing_unchanged` - preserves existing cadence behavior.

## Working Notes
- Planning baseline created.
