# S4.2: OpenFront Worker Query Adapter

**Status**: TODO
**Wave**: W4
**Track**: Main
**Supports**: D4
**Effort**: M
**Dependencies**: S4.1

## Purpose
Preserve current OpenFront worker query messages while preventing them from becoming requirements for Foundation.

## Scope
- In: Adapter for `player_actions`, `player_buildables`, `player_profile`, `player_border_tiles`, `attack_clustered_positions`, and `transport_ship_spawn`.
- In: Tests that existing OpenFront queries still call current `GameRunner` methods.
- Out: Generic query UI for Foundation.

## Touchpoints
- Modify: `src/core/worker/Worker.worker.ts`.
- Modify: `src/core/worker/WorkerMessages.ts`.
- Modify: `src/core/worker/WorkerClient.ts`.

## Acceptance Criteria
- [ ] OpenFront query messages remain available to OpenFront client code.
- [ ] Generic module runtimes are not required to implement OpenFront query methods.
- [ ] Unsupported module queries fail explicitly rather than throwing unrelated OpenFront errors.

## Test Plan
- `openfront_worker_queries_still_route_to_game_runner` - compatibility guard.
- `unsupported_module_query_returns_structured_error` - module boundary guard.

## Working Notes
- Planning baseline created.
