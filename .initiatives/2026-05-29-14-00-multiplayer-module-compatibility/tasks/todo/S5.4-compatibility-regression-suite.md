# S5.4: Compatibility Regression Suite

**Status**: TODO
**Wave**: W5
**Track**: Join
**Supports**: D1, D2, D3, D4, D5
**Effort**: L
**Dependencies**: S5.1, S5.2, S5.3

## Purpose
Collect the compatibility tests needed before non-OpenFront modules can safely use the default remote multiplayer path.

## Scope
- In: Test matrix for OpenFront legacy path, OpenFront generic bridge path, Foundation local path, and Foundation remote service path.
- In: Regression coverage for invalid messages, rate limits, rejoin, turn ordering, hash/desync, minimal archive payloads, and worker update batching.
- Out: Browser E2E coverage unless existing tooling already supports it.

## Touchpoints
- Create: `tests/core/protocol/`.
- Modify: `tests/server/GameLifecycle.test.ts`.
- Modify: `tests/client/LocalServer.test.ts`.
- Modify: `tests/core/systems/WorkerClientUpdateParity.test.ts`.
- Modify: `tests/server/ClientMsgRateLimiter.test.ts`.

## Acceptance Criteria
- [ ] Test suite fails if OpenFront legacy message compatibility breaks.
- [ ] Test suite fails if Foundation remote path bypasses `GameModuleRuntime`.
- [ ] Test suite documents the required checks before a module route is enabled remotely.

## Test Plan
- `openfront_legacy_and_generic_paths_are_equivalent` - migration guard.
- `foundation_remote_multiplayer_uses_default_services` - rollout guard.

## Working Notes
- Planning baseline created.
