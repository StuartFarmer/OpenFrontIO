# S2.2: Play Page And Game Mode Selector

**Status**: DONE
**Wave**: W2
**Track**: Homepage Entry
**Supports**: D2
**Effort**: Medium
**Dependencies**: W1 preferred

## Purpose

Move the homepage play entry area onto HUD stack/grid/action primitives without changing game-start behavior.

## Scope

- In: `PlayPage.ts`, `GameModeSelector.ts`, username surface layout.
- Out: Matchmaking, lobby, or single-player modal internals.

## Touchpoints

- Create: none expected unless a page shell primitive is introduced in S2.1.
- Modify: listed files.

## Acceptance Criteria

- [x] Page wrappers use HUD stack/grid/surface/action group primitives.
- [x] Quick game, solo, create private, and join game actions still validate username and route/open modals correctly.
- [x] Mobile top spacing remains stable.

## Test Plan

- `npx tsc --noEmit --pretty false` - compile.
- Browser smoke: click each homepage action with valid and invalid username states.

## Working Notes

- Planning baseline created.
- Started structural conversion for `PlayPage` and `GameModeSelector`.
- `PlayPage` now uses `hud-stack` for the page root and `hud-toolbar`/`hud-row` for the mobile top bar.
- `GameModeSelector` now uses `hud-stack` and `hud-grid` for the entry actions while preserving the existing username validation and modal/route handlers.
- Updated `hud-grid` so slotted children participate as grid items via `slot { display: contents; }`.
- `npx tsc --noEmit --pretty false` passed after the conversion.
