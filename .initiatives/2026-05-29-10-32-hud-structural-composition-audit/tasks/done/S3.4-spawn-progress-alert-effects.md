# S3.4: Spawn Progress And Alert Effects

**Status**: DONE
**Wave**: W3
**Track**: HUD Overlay
**Supports**: D4
**Effort**: Medium
**Dependencies**: S3.1 preferred

## Purpose

Represent spawn progress and alert-frame effects through HUD primitives or documented effect components.

## Scope

- In: `hud/layers/SpawnTimer.ts`, `hud/layers/AlertFrame.ts`, possibly `ImmunityTimer.ts` and `InGamePromo.ts` if practical.
- Out: Alert detection logic and game update tracking.

## Touchpoints

- Create: possible `hud-progress-strip` primitive.
- Modify: listed files, `HudComponents.ts`, UI kit demo if a primitive is added.

## Acceptance Criteria

- [x] Spawn timer avoids raw visual strip markup where a HUD primitive fits.
- [x] Alert frame is either wrapped as a HUD effect primitive or explicitly documented as an intentional effect exception.
- [x] Animation and timing behavior remain equivalent.

## Test Plan

- `npx tsc --noEmit --pretty false` - compile.
- Manual smoke: spawn phase progress and incoming attack alert path where feasible.

## Working Notes

- Planning baseline created.
- Started by routing spawn/team progress through `hud-meter`; `AlertFrame` remains an intentional full-screen effect unless a dedicated effect primitive is later created.
- `hud-meter` now exposes size/border/background CSS variables so compact strips can reuse it.
- `SpawnTimer` now renders the spawn/team progress as a mini `hud-meter` with segment colors.
- `AlertFrame` was left as an intentional full-screen animation effect because it is not a panel/control surface.
- `npx tsc --noEmit --pretty false` passed.
