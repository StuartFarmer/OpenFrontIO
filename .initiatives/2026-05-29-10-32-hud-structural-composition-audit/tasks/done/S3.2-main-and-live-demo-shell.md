# S3.2: Main And Live Demo Shell Migration

**Status**: DONE
**Wave**: W3
**Track**: Game Shell
**Supports**: D3
**Effort**: Large
**Dependencies**: S3.1

## Purpose

Replace duplicated raw game shell layout in production and demo paths with the new HUD shell primitive.

## Scope

- In: `Main.ts`, `hud/demo/HudLiveComponentsDemo.ts`.
- Out: Controller initialization, game loop, render engine behavior.

## Touchpoints

- Create: none expected.
- Modify: listed files.

## Acceptance Criteria

- [x] `Main.ts` shell markup uses the new HUD shell primitive.
- [x] HUD live demo uses the same primitive.
- [x] Control panel, attacks, unit display, chat/events, sidebars, replay panel, settings, player panel, spawn timer, immunity timer, and build menu remain mounted.

## Test Plan

- `npx tsc --noEmit --pretty false` - compile shell migration.
- Browser smoke: sandbox/solo game HUD placement across desktop and mobile widths.

## Working Notes

- Planning baseline created.
- Started migration of `Main.ts` and `HudLiveComponentsDemo.ts` to `hud-game-shell`.
- `renderSandboxShell` now mounts runtime HUD elements through `hud-game-shell` slots.
- `HudLiveComponentsDemo` now uses the same `hud-game-shell` slot structure.
- Static scan confirmed the old duplicated fixed bottom shell classes are removed from both files.
- `npx tsc --noEmit --pretty false` passed.
