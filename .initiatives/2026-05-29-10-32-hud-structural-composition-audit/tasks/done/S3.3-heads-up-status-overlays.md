# S3.3: Heads-Up Status Overlays

**Status**: DONE
**Wave**: W3
**Track**: HUD Overlay
**Supports**: D4
**Effort**: Medium
**Dependencies**: S3.1 preferred

## Purpose

Move heads-up messages and toast/status presentation to HUD notice/toast primitives.

## Scope

- In: `hud/layers/HeadsUpMessage.ts`.
- Out: Translation keys and game pause/spawn/immunity/catch-up logic.

## Touchpoints

- Create: possible small HUD notice variant if existing primitives cannot match current placement.
- Modify: `HeadsUpMessage.ts`, possibly `HudComponents.ts`, UI kit demo.

## Acceptance Criteria

- [x] Toast messages use HUD primitive styling/composition.
- [x] Spawn/pause/immunity/catch-up message uses HUD primitive styling/composition.
- [x] Existing show-message event behavior and durations are preserved.

## Test Plan

- `npx tsc --noEmit --pretty false` - compile.
- Manual smoke: trigger pause/spawn/toast paths in sandbox or solo game.

## Working Notes

- Planning baseline created.
- Started by replacing raw toast/status message shells with `hud-toast` and `hud-notice`.
- `show-message` toast behavior and timeout logic were left unchanged.
- Spawn/pause/immunity/catch-up status placement remains fixed at the existing screen location, but the visible panel is now `hud-notice`.
- `npx tsc --noEmit --pretty false` passed.
