# S1.3: Modal Validation

**Status**: DONE
**Wave**: W1
**Track**: Validation
**Supports**: D1, D6
**Effort**: Small
**Dependencies**: S1.1, S1.2

## Purpose

Validate modal behavior and document any remaining compatibility wrapper decisions.

## Scope

- In: modal raw usage scans, typecheck, focused smoke notes.
- Out: New modal migrations beyond W1.

## Touchpoints

- Create: optional working notes only.
- Modify: initiative task/wave notes if needed.

## Acceptance Criteria

- [x] `rg "<o-modal\\b|<o-button\\b" src/client -g '*.ts' -n` has no direct app call sites.
- [x] Modal open-count/body-scroll behavior is checked.
- [x] W1 notes list any intentional modal compatibility residue.

## Test Plan

- `npx tsc --noEmit --pretty false` - compile.
- Raw modal scan command - verify no direct legacy usage.

## Working Notes

- Planning baseline created.
- Started validation after S1.2 typecheck passed.
- `BaseModal` now owns modal open-count/body-scroll lock behavior directly while rendering through `hud-modal-shell`.
- Direct legacy app call-site scan is clean.
- Intentional legacy compatibility definitions remain in `src/client/components/baseComponents/Modal.ts` and `src/client/components/baseComponents/Button.ts`; both compose HUD primitives internally.
- `npx tsc --noEmit --pretty false` passed.
- `npx vitest run tests/client/components/UiComponents.test.ts` passed.
- Broader older modal tests still contain assumptions about raw light-DOM internals and should be updated as their owning screens are converted in later waves.
