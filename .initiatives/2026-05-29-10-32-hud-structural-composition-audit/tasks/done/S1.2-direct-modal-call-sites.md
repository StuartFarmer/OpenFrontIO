# S1.2: Direct Modal Call Sites

**Status**: DONE
**Wave**: W1
**Track**: Modal Foundation
**Supports**: D1
**Effort**: Small
**Dependencies**: S1.1

## Purpose
Remove remaining direct `<o-modal>` usage from individual modals.

## Scope
- In: `src/client/hud/layers/ChatModal.ts`, `src/client/GameInfoModal.ts`.
- Out: Chat and game-info business behavior.

## Touchpoints
- Create: none expected.
- Modify: listed files.

## Acceptance Criteria
- [x] Direct `<o-modal>` usage is removed from chat and game info.
- [x] Chat modal still supports team/global selection, send flow, and input focus.
- [x] Game info modal still supports loading, empty state, player rows, and image media.

## Test Plan
- `npx tsc --noEmit --pretty false` - compile direct call-site migration.
- Focused browser smoke: open chat and game info from an active game/sandbox path.

## Working Notes
- Planning baseline created.
- Started implementation after S1.1 typecheck passed.
- Converted `ChatModal` to extend `BaseModal` and render chat content through the HUD modal shell.
- Converted `GameInfoModal` to extend `BaseModal` and render game-info content through the HUD modal shell.
- Direct `<o-modal>` usage scan reports no app call sites after this ticket.
- `npx tsc --noEmit --pretty false` passes after S1.2.
