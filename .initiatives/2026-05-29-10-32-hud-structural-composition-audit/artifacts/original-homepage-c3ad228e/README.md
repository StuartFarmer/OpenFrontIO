# Original Homepage Snapshot

Source commit: `c3ad228e`

This folder keeps the pre-simplification homepage shell and related components for later comparison:

- `index.html`
- `DesktopNavBar.ts`
- `MobileNavBar.ts`
- `PlayPage.ts`
- `MainLayout.ts`
- `Footer.ts`

The active app was restored from this structure, but the current `index.html` intentionally keeps the newer `hud-game-shell` game wrapper so the homepage does not render static in-game HUD elements while outside a running match.
