# S4.1: Help, News, and Ranked Surfaces

**Status**: DONE
**Wave**: W4
**Track**: Docs/News
**Supports**: D4
**Effort**: Medium
**Dependencies**: W3 validation

## Purpose
Convert help/news/ranked app chrome controls and practical help tables to HUD primitives.

## Scope
- In: `HelpModal.ts`, `NewsModal.ts`, `NewsBox.ts`, `RankedModal.ts`.
- Out: Markdown/content images.

## Touchpoints
- Create: none expected
- Modify: listed files

## Acceptance Criteria
- [x] Raw action buttons become HUD buttons.
- [x] Help tables use HUD table atoms where practical.
- [x] Content images remain raw.

## Test Plan
- `npx tsc --noEmit`

## Working Notes
- Planning baseline created.
- Converted News button, NewsBox carousel dots, and Ranked modal card action to HUD primitives.
- `npx tsc --noEmit --pretty false` passes after this partial docs/news/ranked slice.
- Converted `HelpModal` hotkeys and build-menu content tables to `hud-table`, `hud-table-row`, and `hud-table-cell`.
- Added wrapped HUD table cells so longer help/build descriptions do not force single-line tables.
- Corrected quoted Lit event bindings in touched docs/modal surfaces.
- Content screenshots, flags, avatars, and full-color media remain raw `<img>` because `hud-icon` would incorrectly mask them to one color.
