# S4.2: Commerce, Install, and Modal Compatibility

**Status**: DONE
**Wave**: W4
**Track**: Commerce/Compatibility
**Supports**: D4
**Effort**: Medium
**Dependencies**: W3 validation

## Purpose
Convert remaining purchase/cosmetic/install/base modal action controls to HUD primitives.

## Scope
- In: `PurchaseButton.ts`, `CosmeticButton.ts`, `IOSAddToHomeScreenBanner.ts`, `baseComponents/Modal.ts`, `MobileNavBar.ts`.
- Out: Purchase logic, cosmetic media, nav brand image.

## Touchpoints
- Create: possible specialized helper only if purchase effects cannot compose with `hud-button`.
- Modify: listed files

## Acceptance Criteria
- [x] Action controls use HUD primitives.
- [x] Purchase/cosmetic effect behavior remains intact.

## Test Plan
- `npx tsc --noEmit`
- `npx vite build --mode development`

## Working Notes
- Planning baseline created.
- Converted `PurchaseButton`, `CosmeticButton`, `IOSAddToHomeScreenBanner`, `baseComponents/Modal`, and `MobileNavBar` controls to HUD primitives.
- Also converted remaining modal/action compatibility controls in `SinglePlayerModal`, `HostLobbyModal`, `TerritoryPatternsModal`, `TroubleshootingModal`, `WinModal`, `SubscriptionPanel`, and `AccountModal`.
- Converted `FluentSlider` to compose `hud-range` and `hud-input` while preserving editable number behavior.
- Preserved cosmetic media, purchase effects, install copy, nav brand image, and modal behavior.
