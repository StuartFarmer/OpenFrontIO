# S2.4: Lobby Player and Map Picker

**Status**: DONE
**Wave**: W2
**Track**: Lobby
**Supports**: D2
**Effort**: Medium
**Dependencies**: None

## Purpose
Convert lobby player and map picker action controls to HUD primitives.

## Scope
- In: `LobbyPlayerView.ts`, `MapPicker.ts`, `Matchmaking.ts`.
- Out: Map thumbnail images.

## Touchpoints
- Create: none expected
- Modify: listed files

## Acceptance Criteria
- [x] Raw action buttons become HUD buttons.
- [x] Map thumbnail content remains correct.
- [x] Lobby host/kick/ready style behavior is unchanged.

## Test Plan
- `npx tsc --noEmit`
- `npx vite build --mode development`

## Working Notes
- Planning baseline created.
- Converted lobby kick controls to `hud-icon-button`.
- Converted map picker tabs, map cards, and random-map card to `hud-button` while keeping map thumbnails as content.
- Converted matchmaking CTA controls to `hud-button`.
- Validated with raw-control scan, `npx tsc --noEmit`, and `npx vite build --mode development`.
