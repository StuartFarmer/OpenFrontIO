# S2.3: Identity Inputs

**Status**: DONE
**Wave**: W2
**Track**: Lobby Identity
**Supports**: D2
**Effort**: Medium
**Dependencies**: None

## Purpose
Convert username, join lobby, flag, pattern, and language controls where they are app chrome controls.

## Scope
- In: `UsernameInput.ts`, `JoinLobbyModal.ts`, `FlagInput.ts`, `PatternInput.ts`, `LangSelector.ts`, `LanguageModal.ts`.
- Out: Flag/pattern preview media content.

## Touchpoints
- Create: none expected
- Modify: listed files

## Acceptance Criteria
- [x] Raw inputs/buttons are replaced with HUD primitives.
- [x] Preview images remain content images.
- [x] Existing DOM IDs/hooks needed by startup code remain available.

## Test Plan
- `npx tsc --noEmit`
- `npx vite build --mode development`

## Working Notes
- Planning baseline created.
- Converted username, lobby join, flag, pattern, language selector, language modal, and flag modal controls to HUD primitives.
- Extended `hud-input` with length/aria pass-through and styling variables needed by identity fields.
- Preserved `username-input`, `flag-input`, `pattern-input`, `lang-selector`, and `lobbyIdInput` DOM hooks.
- Validated with raw-control scan and `npx tsc --noEmit`.
