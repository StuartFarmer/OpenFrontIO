# S2.1: Game Config Cards

**Status**: DONE
**Wave**: W2
**Track**: Setup
**Supports**: D2
**Effort**: Medium
**Dependencies**: W1 validation

## Purpose
Convert game configuration card/action controls to HUD primitives without changing setup behavior.

## Scope
- In: `GameConfigSettings.ts`.
- Out: Config semantics and API payloads.

## Touchpoints
- Create: possible HUD card/choice helper only if existing primitives cannot represent current controls.
- Modify: `src/client/components/GameConfigSettings.ts`

## Acceptance Criteria
- [x] Raw card buttons replaced with HUD-compatible controls.
- [x] Game mode/config selection behavior is preserved.

## Test Plan
- `npx tsc --noEmit`
- `npx vite build --mode development`

## Working Notes
- Planning baseline created.
- Converted setup card buttons to `hud-button` with card-specific HUD CSS variables.
- Added primitive-level HUD button layout variables for full-width/vertical card cases.
- Validated with `npx tsc --noEmit`.
