# S1.1: Catalog Schema

**Status**: DONE
**Wave**: W1
**Track**: Catalog spine
**Supports**: D1
**Effort**: M
**Dependencies**: None

## Purpose

Define the typed structure for the single HUD component catalog.

## Scope
- In: Component category, status, source path, API metadata, states, CSS variables, examples, migration notes.
- Out: Rendering examples or adding new visual components.

## Touchpoints
- Create: `src/client/hud/ui/HudCatalog.ts`
- Modify: `src/client/hud/ui/index.ts`

## Acceptance Criteria
- [x] Manifest types cover every metadata field required by `docs/HUD_UI_CATALOG_PLAN.md`.
- [x] Status values include `stable`, `draft`, `legacy`, `needs-catalog`, and `deprecated`.
- [x] Type definitions do not import live game-layer types.

## Test Plan
- `npx tsc --noEmit` - confirm manifest types compile.
- `npx eslint src/client/hud/ui` - confirm exports and types pass lint.

## Working Notes
- Implemented in the HUD catalog element pass; verified with `npx tsc --noEmit` and `npx vitest run tests/client/hud/HudCatalog.test.ts`.
