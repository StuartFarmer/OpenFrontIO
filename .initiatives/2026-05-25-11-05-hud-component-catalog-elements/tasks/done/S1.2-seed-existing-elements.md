# S1.2: Seed Existing Elements

**Status**: DONE
**Wave**: W1
**Track**: Catalog spine
**Supports**: D1
**Effort**: M
**Dependencies**: S1.1

## Purpose

Add manifest entries for the current HUD primitives before new elements are introduced.

## Scope
- In: Existing `hud-*` custom elements from `HudComponents.ts`, `relation-smiley`, catalog-only workbench elements as internal/demo-only entries.
- Out: Finished layer composites such as `control-panel` and `leader-board`.

## Touchpoints
- Modify: `src/client/hud/ui/HudCatalog.ts`
- Read: `src/client/hud/ui/HudComponents.ts`, `src/client/hud/layers/RelationSmiley.ts`

## Acceptance Criteria
- [x] Existing atoms, controls, indicators, rows, and surfaces are represented.
- [x] Composite layer components are either omitted or marked out of scope for this initiative.
- [x] Entries include source path and initial API notes.

## Test Plan
- `npx tsc --noEmit` - confirm seeded metadata compiles.
- `git diff --check` - confirm generated tables/strings do not contain whitespace errors.

## Working Notes
- Implemented in the HUD catalog element pass; verified with `npx tsc --noEmit` and `npx vitest run tests/client/hud/HudCatalog.test.ts`.
