# S3.2: Field And Select Controls

**Status**: DONE
**Wave**: W3
**Track**: Form controls
**Supports**: D3
**Effort**: M
**Dependencies**: S2.3

## Purpose

Make text-like inputs and select controls catalog-ready.

## Scope
- In: `hud-input`, `hud-select`, value API, disabled state, option metadata, change events, compact density.
- Out: Multi-step forms and modal-specific validation.

## Touchpoints
- Modify: `src/client/hud/ui/HudComponents.ts`
- Modify: `src/client/hud/ui/HudCatalog.ts`
- Modify: `src/client/hud/demo/HudPanelWorkbench.ts`

## Acceptance Criteria
- [x] Input and select examples cover value, disabled, placeholder, and change behavior.
- [x] API metadata distinguishes attributes from properties.
- [x] Controls remain game-state agnostic.

## Test Plan
- `npx tsc --noEmit` - compile component and catalog changes.
- `npm run start:client` - visual review form control section.

## Working Notes
- Implemented in the HUD catalog element pass; verified with `npx tsc --noEmit` and `npx vitest run tests/client/hud/HudCatalog.test.ts`.
