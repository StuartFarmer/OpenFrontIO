# S8.3: Catalog Validation Guardrails

**Status**: DONE
**Wave**: W8
**Track**: Validation
**Supports**: D6
**Effort**: M
**Dependencies**: S1.1, S1.2

## Purpose

Add tests and checks that keep the catalog coherent as it grows.

## Scope
- In: Custom element registration test, manifest completeness test, duplicate-name guard, example availability guard.
- Out: Full visual regression suite unless project infrastructure already supports it.

## Touchpoints
- Create: `tests/client/hud/HudCatalog.test.ts`
- Modify: `src/client/hud/ui/HudCatalog.ts`
- Read: `src/client/hud/ui/HudComponents.ts`

## Acceptance Criteria
- [x] Tests fail if a catalog entry lacks status, source path, category, or examples.
- [x] Tests fail on duplicate custom element names in the catalog.
- [x] Tests import/register cataloged elements without duplicate-definition errors.

## Test Plan
- `npx vitest run tests/client/hud/HudCatalog.test.ts` - targeted catalog guardrail test.
- `npx tsc --noEmit` - compile the full client after guardrail additions.

## Working Notes
- Implemented in the HUD catalog element pass; verified with `npx tsc --noEmit` and `npx vitest run tests/client/hud/HudCatalog.test.ts`.
