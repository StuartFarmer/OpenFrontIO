# S7.1: Modal And Popover Shells

**Status**: DONE
**Wave**: W7
**Track**: Overlay shells
**Supports**: D5
**Effort**: L
**Dependencies**: S6.1, S3.1

## Purpose

Add reusable overlay shells without building finished modal content.

## Scope
- In: Modal shell, modal header/body/footer, popover shell, backdrop option, close action slot, focus and escape behavior expectations.
- Out: `settings-modal`, `send-resource-modal`, `win-modal`, or `player-info-overlay` migration.

## Touchpoints
- Modify: `src/client/hud/ui/HudComponents.ts`
- Modify: `src/client/hud/ui/HudCatalog.ts`
- Modify: `src/client/hud/demo/HudPanelWorkbench.ts`
- Add: Focus/registration tests if behavior is implemented.

## Acceptance Criteria
- [x] Overlay shells define role, aria, focus, dismissal, and layering expectations.
- [x] Examples use inert sample content only.
- [x] Shells compose existing surface and button primitives where possible.

## Test Plan
- `npx tsc --noEmit` - compile overlay elements.
- `npx vitest run tests/client/hud` - run overlay behavior tests if added.

## Working Notes
- Implemented in the HUD catalog element pass; verified with `npx tsc --noEmit` and `npx vitest run tests/client/hud/HudCatalog.test.ts`.
