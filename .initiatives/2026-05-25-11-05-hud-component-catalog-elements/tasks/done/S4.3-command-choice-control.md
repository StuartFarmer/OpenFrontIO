# S4.3: Command Choice Control

**Status**: DONE
**Wave**: W4
**Track**: Advanced controls
**Supports**: D3
**Effort**: M
**Dependencies**: S3.1, S4.2

## Purpose

Add a small generic choice primitive for command strips without building a game unit selector.

## Scope
- In: A generic command-choice or selectable-item element, icon slot, label slot, count/status slot, selected/disabled/locked state.
- Out: `unit-display` migration and game build logic.

## Touchpoints
- Modify: `src/client/hud/ui/HudComponents.ts`
- Modify: `src/client/hud/ui/HudCatalog.ts`
- Modify: `src/client/hud/demo/HudPanelWorkbench.ts`

## Acceptance Criteria
- [x] Primitive is generic enough for unit, menu, and toolbar choices.
- [x] Catalog examples use sample data only.
- [x] Existing `hud-unit-button` is either documented as specialized or mapped to the generic pattern.

## Test Plan
- `npx tsc --noEmit` - compile new element.
- `npm run start:client` - visual review command choice examples.

## Working Notes
- Implemented in the HUD catalog element pass; verified with `npx tsc --noEmit` and `npx vitest run tests/client/hud/HudCatalog.test.ts`.
