# S1.2: Train Export Blend Payouts

**Status**: DONE
**Wave**: W1
**Track**: Train trade
**Supports**: D2
**Effort**: Small
**Dependencies**: S1.1

## Purpose

Change train stop trade payouts so the old `trainGold` scalar is awarded as a blended total-resource payload based on the train owner's current resources.

## Scope
- In: Update `TradeStationStopHandler` in `src/core/game/TrainStation.ts`.
- In: Update train station unit tests to mock exporter resources and assert blended payloads.
- Out: Do not change `Config.trainGold`.
- Out: Do not change train stats methods.
- Out: Do not change train routing or stop eligibility.

## Touchpoints
- Create: None
- Modify: `src/core/game/TrainStation.ts`, `tests/core/game/TrainStation.test.ts`

## Acceptance Criteria
- [ ] Same-owner train trade uses the train owner's blend and total equals `trainGold(...)`.
- [ ] Allied/team/external station-owner payout uses the train owner's blend and total equals `trainGold(...)`.
- [ ] `trainExternalTrade` and `trainSelfTrade` still receive the scalar trade value.
- [ ] Blend is computed before either recipient is paid.

## Test Plan
- `handles City stop` - update expected payload to blended resources.
- `handles allied trade` - update expected payload to blended resources.
- `records external trade on the station owner` - assert both owners receive sender-blended resources.
- `passes tradeStopsVisited to trainGold` - keep existing regression coverage.

## Working Notes
- Planning baseline created.
- Started implementation.
- Replaced train equal-triplet conversion with a pre-payout train-owner export blend.
- Updated train station tests to assert blended total-resource payloads.
- Verified with `npx vitest run tests/core/game/TrainStation.test.ts`.
- Crash follow-up: train trade now passes `{ updateGold: false }` when awarding non-uniform blended resources so compatibility gold does not reject the payload.
- Visual/log follow-up: train trade now passes a legacy popup amount and emits trade log messages for recipients.
- Resource popup follow-up: train trade now emits the actual Biomass/Fuels/Metals payload for the map popup and resource trade log instead of one combined scalar.
